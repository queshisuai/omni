# Omni 平台改进与优化路线文档

> 状态：当前总路线视图。已完成的阶段性执行项已从本文件删除；详细执行记录见 `progress.md`，关键技术发现见 `findings.md`。

## 当前结论

本地可验证的生产前总路线已经收口。当前不再把已完成阶段作为待办保留，后续只跟踪生产运维后置项和明确二期增强。

边界口径：

- 已完成项不在本文件逐条保留，避免路线文档继续膨胀。
- Sentry/PostHog 真实项目侧 enabled 验收没有声明完成，已后置到生产运维上线前。
- 小程序模块已按用户要求删除，后续总路线不再继续维护小程序整改项。
- 历史证据不删除，仍保留在 `progress.md`、`findings.md` 和对应生产就绪文档中。

## 生产运维后置项

这些事项依赖真实外部项目、真实凭据、外部平台可见性或沙盒稳定性，进入生产运维上线前补充：

1. Sentry 真实 enabled 验收

   - 注入真实 `NEXT_PUBLIC_SENTRY_DSN` / `SENTRY_DSN`、`SENTRY_ENVIRONMENT`、`SENTRY_RELEASE`。
   - 触发少量试点异常。
   - 在 Sentry 项目侧确认事件可见。
   - 核对用户、订单、手机号、token、URL query 等敏感字段已脱敏。
   - 不在仓库写入真实 DSN。

2. PostHog 真实 enabled 验收

   - 配置真实 `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN` 和 `NEXT_PUBLIC_POSTHOG_HOST`。
   - 保持 `autocapture=false`、`capture_pageview=false`、`disable_session_recording=true`、`person_profiles='never'`。
   - 用浏览器 Network 确认 enabled 态存在预期外部请求。
   - 在 PostHog 项目侧确认 allowlist 事件和脱敏属性可见。
   - 不启用 Session Replay，不把自由文本、订单号、用户标识或 URL query 发出。

3. Alipay sandbox 稳定性复测

   - 外部 sandbox 稳定后再单独复测“同意退款”链路。
   - 每次实测后恢复固定 seed 基线，例如 `REFREAL985009`。
   - 不用 mock 支付结果替代真实沙盒链路判断。

## 后续分步总路线

这些不是当前本地生产前收口阻塞项。后续按阶段单独开任务、设计验收标准和验证命令，不把多个阶段混在一次改动里。

### 阶段 11：C 端可解释体验补强

目标：让普通用户看得懂候补、抢票、订单、票夹、退款和通知状态，减少裸露技术 ID 和“失败但不知道为什么”的体验。

优先范围：

- 候补页去 ID 化：把 `sessionId`、`ticketTypeId` 替换为活动名、场次时间、票档名、场馆、订单入口和失败原因解释。
- 小队抢票去 ID 化：房间页补活动、场次、票档、成员确认、座位策略和最新订单摘要。
- 订单/票夹/退款时间线：在订单详情和票夹展示支付同步、退款申请、审核、到账、入场码、转赠和核验状态。
- 搜索与推荐体验：在现有筛选基础上补搜索建议、空结果真实推荐、近期浏览召回、订阅艺人/开票提醒入口。
- 通知动作闭环：通知卡片继续保持“一点即处理”，候补、退款、客服、核验、风险待办都跳到具体业务页。

第一轮验收：

- 普通用户账号能从首页进入活动、候补、小队、订单、票夹、通知和客服，不出现裸露业务 ID 作为主要信息。
- 失败态、空状态、权限态和加载态均为中文。
- 前端 `pnpm typecheck` 通过，相关页面浏览器 console 无 error/warn。

### 阶段 12：主办方经营闭环

目标：把主办方后台从“能发布和查看”升级成“能经营、能分析、能导出、能批量处理”。

优先范围：

- 主办方经营驾驶舱：按活动/场次展示票房、销量、退款率、入场率、库存热区、候补转化、支付超时和抢票失败原因。
- 报表导出升级：在现有 CSV 基础上补 Excel/PDF、场次维度报表、退款明细、核验异常报表和日结对账单。
- 批量运营：批量上下架、批量改价、批量调整票档、批量通知购票用户、批量处理退款和批量导入座位/票档。
- 活动发布工作流：草稿完整度、待补材料、审核阻塞点、上线检查清单、站点变更状态集中展示。
- 营销工具增强：优惠券/满减之外补活动人群、转化漏斗、优惠效果、复购和粉丝画像。

第一轮验收：

- 主办方账号能在 `/console` 看到至少一个活动维度经营摘要，不需要跳多个页面拼信息。
- 订单、退款、核验至少有一种可下载报表，且不导出完整证件号、token 或敏感字段。
- 批量动作必须有二次确认、结果反馈和操作审计。

### 阶段 13：平台治理与运营中台

目标：平台方不只“能管理”，还要能主动发现风险、定位异常、追踪服务健康和控制权限边界。

优先范围：

- 平台健康看板：GMV、支付成功率、退款异常率、库存锁失败率、抢票失败原因、Gateway 5xx/超时、Nacos/Seata/Redis/RabbitMQ 状态。控制台首页已展示 ticket/payment/grab/workbench 摘要链路健康，并已将 Nacos、Redis、RabbitMQ、Seata 拆为独立“基础设施健康”探针；缺少探针字段时显示未配置，不把摘要链路健康等同于基础设施健康。
- 风控规则台：同设备、同账号、同证件、异常退款、批量抢票、异常下架、恢复售票等规则先做只读命中日志，再做人工复核动作。
- 异常与对账二期：补复核归档、处理说明、处理人、更新时间和批次完成条件；更细审计筛选已完成第一轮中文下拉入口，审计对象类型已继续补齐艺人档案等实际审核场景映射。
- 后台中文化与枚举展示治理：站点变更类型与配置状态、审计对象类型、艺人档案审核状态、异常任务类型与队列操作状态、评价审核/举报/问答状态、场馆资料审核状态、主办方入驻审核状态、主办方账号状态、客服账号状态、平台主办方运营员账号状态、恢复售票审核状态、对账摘要字段、对账历史批次码值和对账差异状态等不得直接展示 `change_schedule`、`submitted`、`station_config_version`、`PAYMENT_TIMEOUT`、`paidOrderCount`、`REFUND_AMOUNT_MISMATCH` 这类代码值；统一映射为中文业务标签，站点变更审核页已展示中文状态并保护非待审核状态，异常任务新建入口已复用共享类型映射，异常任务队列已对未知或未来状态显示“状态待核对”并阻断认领/处理/关闭写动作，艺人档案审核页已显示中文审核状态并在未知状态下阻断通过/拒绝/标记风险入口，评价审核和购前问答事件入口已在未知或未来状态下阻断 moderation 写动作，评价举报未知状态已显示“状态待核对”并阻断处理/驳回入口，场馆资料审核未知状态已显示“状态待核对”并阻断审核/通过/驳回入口，主办方入驻审核未知状态已显示“状态待核对”并阻断通过/驳回入口，取消主办方入口已在未知主办方状态下显示“状态待核对”并阻断写动作，客服账号和平台主办方运营员账号未知状态已阻断编辑和启停入口，恢复售票审核未知状态已显示“状态待核对”并阻断通过/拒绝入口，风险事件待办页已对恢复售票申请未知或审核中状态提供中文阻断反馈和提交前二次复核，对账详情和导出已兼容历史业务类型、明细状态与差异类型码值，对账差异未知状态已显示“状态待核对”并阻断处理/忽略入口，对账批次摘要已兼容中英文 key，必要的技术追踪字段只保留中文列名和中文说明。
- 退款审核写动作保护：单条同意/重试和拒绝入口已按当前退款记录状态二次复核，未知、已结束或动作不匹配状态会显示“退款状态待核对”并阻断写接口。
- 客服会话写动作保护：客服工作台和 C 端帮助中心均已复用会话状态 helper；未知或未来会话状态会显示“会话状态待核对”并阻断回复、编辑、发送、转人工或确认结束等写动作；客服工作台已知但当前动作不可执行的状态也会给出中文阻断提示，不再静默失败。
- 控制台入口守卫：普通主办方即使携带 `permissionCodes`，仍必须按主办方业务路径守卫访问 `/console/risk-events` 等 organizer 路由，不能被平台权限码过滤误重定向到控制台首页。
- 客服主管能力：SLA、满意度、质检评分、排班、接待容量、问题分类统计和 FAQ 反哺。
- 平台主办方运营二期：高风险双人复核、按负载自动分配、主办方活动/订单/退款摘要。

第一轮验收：

- 平台管理员能在一个页面看到服务健康、交易健康、退款异常、抢票异常和待处理风险。
- 站点变更审核、日志列表、异常任务队列、对账批次列表不出现未解释的英文枚举名或后端字段名；保留订单号、批次号、TraceId 等技术标识时必须有中文业务语境。
- 所有高风险写动作必须写 `operation_audit_log` 或对应业务审计。
- 普通主办方、普通客服和普通用户直接访问平台治理接口必须返回权限不足。

### 阶段 14：生产运维与外部能力增强

目标：在不污染仓库、不暴露敏感数据的前提下，把外部监控、分析、短信邮件和网关治理逐步接入真实环境。

优先范围：

- Sentry：完成真实 DSN enabled 态项目侧验收，确认 release、route 归一、脱敏和关闭策略。
- PostHog：完成真实 token/host enabled 态项目侧验收，只发送 allowlist 事件，继续关闭 autocapture 和 Session Replay。
- 通知渠道：真实 SMS/Email 适配器、投递记录、重试、失败告警和通道开关。
- Gateway：生产链路追踪、route 级延迟指标、限流、熔断和慢链路诊断页。
- 链路耗时治理：把 AI 客服首字响应、模型回落原因、抢票排队/锁票/建单/支付同步/出票各阶段耗时拆开记录；避免把下游模型推理、订单服务调用或进度轮询问题误判为网关问题。
- ES 搜索：活动检索、索引重建、搜索建议、结构化筛选与排序；ES 不可用时不得回退 mock 活动。
- 外部工具候选：Clerk/Cloudflare/Resend/Upstash/Pinecone 等只作为能力池，接入前必须重新评估用途、成本、退出方案和数据边界。

第一轮验收：

- 禁用态无外部网络写入，启用态只向授权项目发送脱敏数据。
- 所有真实密钥、token、DSN、host 只通过环境变量注入，不写入仓库。
- 外部依赖失败时，登录、浏览、购票、支付、退款、客服和控制台主路径不被阻断。
- 网关、前端代理、下游服务、数据库、Redis、RabbitMQ 和本地模型的耗时必须分别留证；只升级网关不能作为 AI 客服或抢票出票慢的完整解决方案。

## 保留原则

- 不恢复动态系统、`MomentSection`、`SocialController`、moment API 或旧 social/moment 持久化边界。
- 不恢复生产入口的 mock/offline 降级，不用假数据伪装真实活动、支付、通知或推荐。
- 不在仓库写入真实密钥、token、DSN、host 或外部账号凭据。
- 不用 mock DSN、占位 token、假 transport 或本地日志替代外部 SaaS 项目侧验收。
- 涉及数据库结构变更时，必须同步 SQL 迁移、本地数据库执行和验证命令。
- 后端能力要同步前端入口、状态、权限、错误提示和浏览器验证。
- 每个新范围只选择一个清晰任务推进，不把二期增强重新混回当前总路线。

## 参考文档

- `task_plan.md`
- `progress.md`
- `findings.md`
- `docs/production-readiness/production-defaults-audit.md`
- `docs/production-readiness/sentry-evaluation-and-trial-plan.md`
- `docs/production-readiness/posthog-evaluation-and-trial-plan.md`
- `docs/production-readiness/production-env-vars.md`
