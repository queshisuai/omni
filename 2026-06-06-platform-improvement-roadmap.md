# Omni 平台改进与优化路线文档

> 状态：生产前路线规划文档。当前项目尚未进入生产，允许对不合理设计做大改、替换和重新命名，但必须分阶段实施、保留迁移资产，并在每个阶段完成验证。

## 目标

把 Omni 从“核心链路可跑通”推进到“高频真实运营可用”：

- C 端用户能搜索到真实、及时、可解释的票务信息。
- 主办方能高效发布、运营、分析活动。
- 客服能在一个工作台内看见订单、退款、抢票、候补、票夹和通知上下文。
- 平台管理员从全能操作员转向治理者，日常主办方运营由平台主办方运营员分担。
- 中间件各司其职：ES 做检索，MQ 做事件解耦，Redis 做高频状态和限流缓存，Seata 用于必要的跨服务一致性，Gateway 升级为可观测、可治理的统一入口。
- 外部 SaaS 和免费工具按“候选能力池”管理，先评估、再小步接入，不一次性替换现有核心链路。
- 生产前发现不合理、不适合长期维护或不适合真实运营的实现，应及时提出并替换，不为了兼容临时代码牺牲长期质量。

## 范围校准

### 评价系统

评价系统重新纳入项目范围，但边界需要重新定义：

- 允许：活动详情评价、购后观演反馈、购前问答、评价审核、评价举报。
- 禁止：恢复旧动态系统、`MomentSection`、`SocialController`、moment API、旧 social/moment 持久化边界。
- 命名要求：新评价能力应围绕 `activity review`、`activity question` 或明确的活动互动模型，不再混用旧社交动态概念。

### 通知系统

当前客服回复走 MQ 触发站内通知是正确方向。后续缺口不是站内通知，而是统一通知事件模型和 SMS 渠道接入。

- 站内通知：所有关键业务事件必须覆盖。
- SMS：作为可插拔渠道接入，未接入时不影响站内通知。
- MQ：作为业务事件到通知投递的解耦层。

### 主办方管理员

当前“主办方管理员”定位调整为平台级主办方运营角色，不是某个主办方租户下的管理员。

建议后续改名：

- `organizer_ops_agent`：主办方运营专员
- `organizer_ops_manager`：主办方运营主管

如果暂时保留 `organizer_admin`，前端中文显示建议改为“平台主办方运营员”，避免被误解为主办方内部管理员。

### 外部 SaaS / 工具候选

这些工具可作为后续提效和生产化候选，但不能一次性接入，也不能让核心业务强依赖不可控免费额度。

| 工具 | 定位 | 接入建议 |
|:---|:---|:---|
| GitHub | 版本控制、Issue、PR、Actions | 继续作为代码协作基础，不改变项目“不自动提交/推送”的协作纪律。 |
| Resend | 邮件服务 | 可作为通知中台的 Email 渠道适配器候选，不直接写进业务服务。 |
| Clerk | 用户认证和用户管理框架 | 先评估，不直接替换 `java-user`；如果接入，Clerk 管认证会话，本地用户服务保留业务用户、角色、权限、实名观演人等领域数据。 |
| Cloudflare | DNS、CDN、WAF、边缘能力 | 可用于域名、静态资源、基础防护和缓存策略。 |
| PostHog | 产品数据分析 | 可用于前端曝光、详情访问、搜索转化、按钮点击和漏斗分析。 |
| Sentry | 前后端错误监控 | 可用于前端异常、Gateway 异常、服务端异常聚合和 release 追踪。 |
| Upstash | Redis 托管服务 | 可作为云端轻量 Redis 候选；本机和当前服务仍优先沿用现有 Redis。 |
| Pinecone | 向量数据库 | 可用于客服知识库、活动语义搜索、FAQ 召回；不替代 ES 的结构化搜索。 |

Clerk 评估口径：

- Clerk 适合承担登录、注册、第三方登录、会话管理、前端认证组件和 session token 签发。
- 现有平台已经有用户、角色、RBAC、客服账号、主办方运营角色、实名观演人和 internal token 边界，因此 Clerk 不应直接替代全部用户服务。
- 如果试点 Clerk，建议采用“双层模型”：Clerk 负责身份认证，本地 `java-user` 负责业务用户档案、角色权限和微服务 internal API。
- Gateway 或用户服务需要通过 Clerk JWT/JWKS 校验外部用户身份，再映射为本地 `userId`。
- 接入前必须先设计账号绑定、手机号冲突、历史用户迁移、退出登录、封禁停用和审计策略。

### 数据库与种子数据纪律

当前本地数据库主要是种子数据，用于演示、联调和验收。后续如果发现数据不合理，可以调整，但必须区分“结构迁移”和“种子数据”。

- 表结构、字段、索引、约束变更必须写 SQL 迁移，不能只改 Entity、DTO 或 Mapper。
- 拆库环境下迁移文件应放到对应服务目录，例如 `sql/production-split/user`、`sql/production-split/ticket`、`sql/production-split/order`。
- 本地数据库需要同步执行迁移，避免代码和本地库结构不一致。
- 种子数据调整应放在 seed 或 demo seed 资产中，不能混进生产结构迁移。
- 改字段时必须同步检查前端类型、后端 DTO、Mapper XML、测试数据和现有本地数据。
- 任何数据库变更都要记录影响范围、回滚方式和本地验证命令。

### 前后端闭环纪律

后端测试通过不等于用户可用。后续每个阶段都必须补一轮前端入口和交互闭环检查。

- 每个后端能力都要确认是否有前端页面、入口按钮、空状态、加载态、错误态和权限态。
- 如果后端已有接口但前端没有入口，要补入口；如果入口按钮存在但没接真实接口，要接通；如果暂不支持，要隐藏或明确禁用。
- `frontend/src/lib/api.ts` 和 `frontend/src/types/api.ts` 要跟后端响应保持一致。
- 控制台菜单、C 端详情页、订单页、票夹、客服页都要按角色验证。
- 前端改动后必须用浏览器实际打开页面验证关键流程，不能只看后端测试。
- 文案必须中文；不能把后端枚举、异常码直接裸露给普通用户。

### 生产前大改原则

当前仍处于生产前准备阶段，架构和功能可以大改，但要按以下原则执行：

- 可以替换临时实现，不为 demo 代码背长期包袱。
- 可以调整角色命名、权限边界、接口路径和表结构，但必须提供迁移和前后端同步修改。
- 可以引入 ES、MQ、Redis、Sentry、PostHog、Clerk 等能力，但每个外部依赖都要能关闭、降级或替换。
- 不合理的地方要直接指出，不用为了保持现状而绕路实现。
- 大改必须拆小阶段，每阶段都能独立验证。
- 涉及数据库结构时必须先写迁移，再执行本地数据库迁移，再改后端和前端。
- 涉及前端入口时必须实际浏览器验证，不只依赖后端测试。

### 当前优先可替换清单

| 当前状态 | 建议替换方向 | 原因 |
|:---|:---|:---|
| 活动搜索内存过滤和分页 | ES 活动搜索索引 | 高频搜索、筛选、排序和分页必须稳定准确。 |
| 轻量级 Gateway | 可观测、可限流、可鉴权、可诊断 Gateway | 当前服务联动慢需要先定位，再升级网关治理能力。 |
| 沙盒短信和固定验证码 | 通知事件中台 + SMS 渠道适配器 + Redis 验证码 | 抢票、退款、改期、登录都需要真实通知能力。 |
| `organizer_admin` 命名 | 平台主办方运营员 / `organizer_ops_agent` | 避免误解为主办方租户管理员。 |
| 后端已有、前端未体现 | 前端入口审计和补齐 | 用户看不到的后端能力等于没有完成。 |
| mock 搜索和首页降级 | 真实失败态、重试、真实缓存 | 生产前必须避免假活动误导用户。 |
| 过大的 `frontend/src/lib/api.ts` | 按领域拆分 API 客户端和类型 | 后续功能多，单文件会越来越难维护。 |
| 默认 JWT 密钥、分散鉴权 | Gateway/Filter 统一鉴权和密钥强校验 | 生产前必须收紧安全边界。 |
| 残留评价/问答实现 | 正式评价系统模块 | 既然评价重新纳入，就要补审核、权限、订单校验和前端入口。 |
| 演示种子数据 | 场景化种子数据集 | 需要支撑用户、主办方、客服、管理员完整演示。 |

### 暂不建议替换的基础

- PostgreSQL 继续作为权威业务数据源，ES、Redis、Pinecone 都不替代关系型主库。
- 五库拆分和微服务边界继续保留，不回退到共享大库。
- `Result<T>` 统一响应继续保留，避免前后端协议大面积震荡。
- Seata 不滥用，只放在确实需要跨服务一致性的票务、订单、支付关键链路。
- 本地 Docker PostgreSQL 不恢复，继续使用宿主机五库联调。

## 分阶段路线

## 阶段 0：规则与基线整理

目标：先把规则、文档、命名口径统一，避免后续实现时反复摇摆。

主要工作：

- 更新项目规则：评价系统重新纳入范围，动态系统仍禁止恢复。
- 明确主办方管理员是平台级运营角色。
- 确认当前网关、搜索、通知、客服、评价相关代码现状。
- 输出生产前可替换清单，标记“保留、替换、重命名、延后”。
- 输出前端入口审计清单，标记后端已有但前端未体现的功能。
- 输出种子数据审计清单，标记不合理、不完整或无法支撑演示的数据。
- 建立后续每阶段的验证命令清单。

验收标准：

- `AGENTS.md` 不再禁止评价系统。
- 文档明确禁止恢复动态系统。
- 可替换清单、前端入口清单、种子数据清单有明确归档位置。
  - `docs/production-readiness/replacement-audit.md`
  - `docs/production-readiness/frontend-entry-audit.md`
  - `docs/production-readiness/seed-data-audit.md`
  - `docs/production-readiness/check-in-flow.md`
  - `docs/production-readiness/check-in-sync-implementation-plan.md`
- 后续阶段可以按本文拆分执行。

## 阶段 1：ES 活动搜索改造

目标：把活动搜索从临时内存过滤迁移到 Elasticsearch。

中间件使用：

- ES：活动检索、筛选、排序、搜索建议。
- MQ：活动、场次、票档、艺人、场馆变更后投递索引更新事件。
- Redis：热门搜索词、搜索结果短缓存、搜索限流。

建议设计：

- DB 仍是权威数据源，ES 是查询索引。
- 新增 `ActivitySearchService` 抽象，ES 实现作为主路径。
- ES index 使用版本化命名，例如 `omni_activity_v1`，通过 alias `omni_activity_current` 查询。
- 活动搜索文档建议包含：
  - `activityId`
  - `tourId`
  - `organizerId`
  - `activityName`
  - `artistName`
  - `categoryId`
  - `categoryName`
  - `city`
  - `venueName`
  - `startTime`
  - `minPrice`
  - `maxPrice`
  - `saleStatus`
  - `seatMapVisibility`
  - `realNameRequired`
  - `subscriptionCount`
  - `paidOrderCount`
  - `hotScore`
- 查询支持关键词、分类、城市、日期、价格、销售状态、是否选座、是否实名、排序。
- 提供全量重建索引脚本和按活动重建接口。
- 前端搜索失败时显示真实失败态，不再使用 mock 活动作为生产降级。

验收标准：

- 搜索接口结果来自 ES。
- 分页总数准确。
- 活动变更后 ES 文档能被更新。
- ES 不可用时返回明确错误或受控降级，不返回假活动。

## 阶段 2：通知事件中台与 SMS 渠道

目标：让抢票、退款、改期、客服回复等关键事件统一产生站内通知，并预留 SMS 接入。

中间件使用：

- MQ：通知事件传递、重试队列、死信队列。
- Redis：幂等去重、短期频控、短信验证码缓存。

建议设计：

- 定义统一通知事件：
  - `SUPPORT_REPLY`
  - `GRAB_SUCCESS`
  - `GRAB_FAILED`
  - `WAITLIST_MATCHED`
  - `REFUND_APPROVED`
  - `REFUND_REJECTED`
  - `ACTIVITY_RESCHEDULED`
  - `ACTIVITY_CANCELLED`
  - `ORDER_PAYMENT_TIMEOUT`
- 事件字段建议包含：
  - `eventId`
  - `aggregateKey`
  - `userId`
  - `orderId`
  - `activityId`
  - `templateCode`
  - `channels`
  - `priority`
  - `payload`
- 站内通知渠道默认启用。
- SMS 渠道使用接口适配器，先定义接口和投递记录，供应商后接。
- 每次投递记录渠道、状态、失败原因、重试次数。

验收标准：

- 客服回复、退款、抢票、改期至少有站内通知。
- 同一个业务事件不会重复刷屏。
- SMS 未配置时不影响站内通知。

## 阶段 3：Gateway 升级与链路提速

目标：把轻量级网关升级为可观测、可限流、可诊断的统一入口，并定位服务联动慢的真实原因。

先诊断，不直接假设全部是网关问题：

- 对比直连服务和经 Gateway 的延迟。
- 统计 Gateway route 级别 p50、p95、p99。
- 记录慢请求 traceId、routeId、目标服务、耗时阶段。
- 检查 Nacos 服务发现、负载均衡、连接池、超时、Netty event loop 是否存在阻塞。

中间件使用：

- Redis：网关限流、热点接口保护、短期认证上下文缓存。
- MQ：不放在同步请求链路里，避免把网关变成业务编排器。
- Nacos：服务发现和配置。

建议升级能力：

- 全局 traceId。
- route 级耗时日志。
- route 级超时配置。
- 熔断和快速失败。
- Redis 限流。
- CORS、鉴权、内部接口保护统一化。
- 对抢票、支付、客服 SSE 等长耗时接口配置单独策略。

验收标准：

- 可以看见每个 route 的耗时分布。
- Gateway 额外耗时有明确基线。
- 高频接口不再因为无差别超时策略互相影响。
- 慢链路能定位到 gateway、服务、DB、Nacos 或下游调用。

## 阶段 4：平台主办方运营员工作台

目标：让平台主办方运营员承担主办方日常管理，减轻平台管理员负担。

建议角色：

- 主办方运营专员：跟进主办方、查看业务数据、处理入驻资料、提交处理建议。
- 主办方运营主管：审核高风险动作、分配运营专员、复核停用或恢复操作。
- 平台超管：保留 RBAC、对账、异常补偿、系统配置等高危权限。

建议工作台能力：

- 主办方列表：状态、风险等级、入驻资料、活动数量、订单规模。
- 待办队列：入驻审核、资料补充、风险整改、活动异常、退款异常。
- 跟进记录：备注、处理人、下次跟进时间。
- 分配机制：按城市、品类、风险等级或运营员负载分配。
- 高风险动作双人复核：停用主办方、恢复主办方、下架活动、批量退款。

验收标准：

- 主办方运营员能完成日常主办方管理。
- 平台管理员不需要处理所有主办方琐事。
- 高危动作有审计和复核。

## 阶段 5：客服上下文工作台

目标：客服处理问题时不再多页面跳转。

建议能力：

- 会话右侧聚合用户最近订单、退款、票夹、候补、抢票请求、通知记录。
- 一键跳转订单详情、退款详情、抢票进度。
- 常见问题知识库维护。
- AI 回复引用知识库和订单上下文。
- 客服主管查看 SLA、满意度、质检抽检、排班和接待容量。

中间件使用：

- MQ：客服回复站内通知。
- Redis：在线状态、接待容量、会话轮询优化。

验收标准：

- 客服能在一个页面判断用户问题来源。
- 人工客服回复能稳定触发站内通知。
- 客服主管能看到处理质量和队列压力。

## 阶段 6：评价系统正式化

目标：把活动评价从残留功能升级为正式模块。

建议能力：

- 购票后评价。
- 活动详情展示评分、评价列表、图片。
- 评价审核、举报、隐藏。
- 问答区保留为购前问答，不归入动态系统。
- 订单页提供评价入口。

边界要求：

- 不恢复动态系统。
- 不恢复旧 `SocialController`。
- 不恢复 moment API。
- 评价数据归属票务服务，订单校验通过订单 internal API 完成。

验收标准：

- 项目规则、接口命名、前端展示一致。
- 未购票用户不能发布购后评价。
- 平台可以审核或隐藏违规评价。

## 阶段 7：运营分析与异常闭环

目标：让平台运营从“看数据”升级为“处理问题”。

建议能力：

- 搜索曝光、详情访问、想看、候补、下单、支付、退款漏斗。
- 抢票失败原因分析。
- 支付超时率、退款异常率、风控命中率。
- 异常任务认领、处理、重试、关闭。
- 对账差异处理、复核、归档。

中间件使用：

- MQ：业务事件采集。
- ES：搜索和运营检索。
- Redis：热点统计短缓存。

验收标准：

- 异常任务有明确状态机。
- 对账差异可以闭环处理。
- 平台管理员能从总览页进入处理页面。

## 阶段 8：外部 SaaS 评估与试点接入

目标：把 Clerk、Resend、PostHog、Sentry、Cloudflare、Upstash、Pinecone 等候选工具纳入可控评估，而不是一次性替换现有系统。

建议顺序：

1. Sentry：先接前端错误监控和 release 标识，低侵入、收益直接。
2. PostHog：接前端埋点，服务 ES 搜索和运营漏斗。
3. Resend：作为通知中台 Email 渠道适配器，不直接耦合业务服务。
4. Clerk：做认证试点方案，先设计账号映射和本地用户兼容，不直接替换登录。
5. Pinecone：用于客服知识库语义召回试点。
6. Cloudflare：在部署和域名阶段接入 DNS、CDN、基础防护。
7. Upstash：仅在云端轻量部署需要托管 Redis 时评估。

验收标准：

- 每个 SaaS 都有独立接入文档、环境变量清单和关闭方案。
- 密钥不写入仓库，不在日志中输出。
- 免费额度耗尽时，核心购票链路不崩溃。

## 阶段 9：前端入口与种子数据治理闭环

目标：解决“后端已有、测试也过，但前端没有体现”的断层。

主要工作：

- 按角色列出现有后端能力清单：普通用户、主办方、客服、客服管理员、平台主办方运营员、平台管理员。
- 对照前端路由、菜单、按钮和页面状态，标记“已接通、半接通、缺入口、仅 mock、需隐藏”。
- 对种子数据做一次真实性审计，补齐能支撑前端演示的活动、订单、退款、票夹、客服、评价、通知、抢票、候补数据。
- 补齐入场核验同步流程：C 端票夹展示动态入场码；线下闸机、扫码设备或工作人员 App 是主流程；平台侧提供入场概览、核验记录、异常补录和可选备用 Web 扫码页。
- 若发现字段或表结构不支撑前端展示，先写迁移，再执行本地数据库迁移，再接前端。
- 为每个角色整理一条可手工验证的用户旅程。

验收标准：

- 前端入口和后端接口一一对应。
- 关键按钮没有空点击或假成功。
- 本地种子数据能支撑完整演示链路。
- 入场核验不依赖常规 Web 工作台主流程，后台能追溯核验记录和异常同步状态。
- 数据库结构变更已迁移到本地库并有 SQL 资产。

## 阶段 10：生产前全链路整顿验收

目标：在正式生产部署前，集中清理临时实现、假数据入口、安全弱点和慢链路。

主要工作：

- 清理或替换所有 mock/offline 降级。
- 检查所有默认密钥、固定验证码、沙盒实现。
- 对 C 端购票、抢票、候补、支付、退款、票夹、客服、评价做端到端验证。
- 对 B 端主办方发布、场次、票档、座位图、营销、退款处理、风险处理、入场核验同步做端到端验证。
- 对平台后台 RBAC、审计、异常任务、对账、主办方运营工作台做端到端验证。
- 输出生产前问题清单，按 P0/P1/P2 排序处理。

验收标准：

- 没有会误导用户的 mock 数据。
- 没有默认密钥或固定验证码进入生产配置。
- Gateway 慢链路有指标和定位方式。
- 每个角色都有可跑通的前端用户旅程。
- 本地五库结构、迁移脚本、种子数据和代码一致。

## 执行纪律

- 不一次性实施全部内容。
- 每次只选一个阶段或一个阶段内的一个小任务。
- 生产前允许大改和替换，但每次大改必须写清影响范围、迁移方案、验证命令和回退思路。
- 每个阶段先补测试或验证脚本，再改实现。
- 后端改动后必须编译相关服务。
- 网关改动必须提供直连与经 Gateway 的延迟对比。
- 涉及下载依赖、ES 客户端、短信 SDK 或大规模联网操作时，先说明下载内容和推荐镜像源，得到确认后再执行。
- 涉及 Clerk、Resend、PostHog、Sentry、Cloudflare、Upstash、Pinecone 等外部服务写入或绑定账号前，必须先确认用途、环境变量、免费额度、退出方案和影响范围。
- 不自动提交，不自动推送。

## 建议下一步

优先执行阶段 0，然后进入阶段 1：

1. 完成规则更新。
2. 为 ES 搜索写详细实施计划。
3. 小步接入 ES 搜索，不动通知、客服、主办方运营工作台。
4. 搜索稳定后，再进入通知事件中台。
5. 并行做一份前端入口审计清单，标出“后端已完成但前端未体现”的功能。
