# Omni 万象抢票平台答辩 PPT 计划流程书

## 1. 基本信息

- PPT 主题：Omni 万象抢票平台
- 使用场景：课程设计 / 毕业设计 / 软件工程项目答辩
- 建议比例：16:9 横版
- 建议页数：20 页
- 建议答辩时长：10-12 分钟
- 项目定位：基于 Spring Cloud Alibaba 与 Next.js 的微服务票务平台

## 2. 整体视觉风格

| 项目 | 建议 |
|---|---|
| 主视觉 | 现代互联网产品 + 演唱会票务场景 |
| 主色 | `#ff1268` |
| 辅色 | 深蓝黑、紫色、霓虹蓝 |
| 背景 | 深色科技感背景，局部使用舞台灯光、票券、城市霓虹元素 |
| 字体风格 | 标题粗体、大字号；正文简洁；技术页使用清晰线框图 |
| 图形风格 | 卡片式布局、微服务节点图、流程箭头、数据库分库图 |
| 关键词 | 抢票、演唱会、微服务、分布式、高并发、购票流程 |

通用生图风格前缀：

```text
16:9 横版高校软件项目答辩 PPT，主题为“Omni 万象抢票平台”，现代互联网产品风格，深色科技背景，主色为玫红色 #ff1268，辅色为蓝紫色和霓虹蓝，包含演唱会票务、电子票券、微服务架构、数据流、服务器节点等视觉元素。页面布局清晰，适合放中文标题和说明文字，专业、简洁、有科技感，不要过度复杂，不要遮挡文字区域。
```

## 3. 团队分工

项目共 6 人合作完成，建议按“业务与数据、后端与中间件、前端与演示”三条线分工，每 2 人负责一组。

| 小组 | 成员 | 负责方向 | 技术关键词 |
|---|---|---|---|
| 第 1 组 | 成员 A、成员 B | 业务需求、业务流程、数据库设计、缓存设计 | 需求分析、PostgreSQL、Redis |
| 第 2 组 | 成员 C、成员 D | 后端架构、微服务治理、核心业务链路 | Spring Cloud、Nacos、OpenFeign、Sentinel、Seata |
| 第 3 组 | 成员 E、成员 F | 前端展示、搜索通知、系统演示、测试总结 | Next.js、ES、MQ、系统演示 |

具体成员职责：

| 成员 | 负责内容 | 负责中间件 / 技术点 |
|---|---|---|
| 成员 A | 项目背景、业务痛点、系统角色、项目目标 | 无需重点讲中间件 |
| 成员 B | 需求分析、业务流程、数据库设计、缓存设计 | Redis |
| 成员 C | 系统总体架构、微服务拆分、网关、服务注册发现 | Spring Cloud、Nacos |
| 成员 D | 服务间通信、订单/支付/票务核心链路、服务边界 | OpenFeign、Sentinel、Seata |
| 成员 E | 前端页面、用户端功能、B 端功能、活动搜索 | Next.js、ES |
| 成员 F | 系统演示、测试验证、消息通知、总结展望 | MQ |

中间件讲解分配：

| 中间件 | 负责人 | 答辩讲解角度 |
|---|---|---|
| Spring Cloud | 成员 C | 微服务基础框架，支撑 user、ticket、order、payment、notification 等服务拆分 |
| Nacos | 成员 C | 服务注册与发现，Gateway 根据服务名进行路由转发 |
| OpenFeign | 成员 D | 服务间 HTTP 调用，例如 order 调用 user 校验用户、调用 ticket 锁定库存 |
| Sentinel | 成员 D | 限流、熔断、降级，保护抢票、下单、支付等高并发接口 |
| Seata | 成员 D | 分布式事务一致性设计，用于解释订单、库存、支付之间的一致性问题 |
| Redis | 成员 B | 缓存活动信息、票档库存、抢票锁、防重复提交 |
| MQ | 成员 F | 异步通知、订单状态变更、支付结果通知、削峰解耦 |
| ES | 成员 E | 活动标题、地点、主办方、分类等全文检索 |

## 4. PPT 章节结构

| 章节 | 页码 | 内容 |
|---|---|---|
| 第一章：项目概述 | 1-4 | 封面、团队分工、项目背景、项目目标 |
| 第二章：需求与业务设计 | 5-6 | 需求分析、核心业务流程 |
| 第三章：系统架构与技术实现 | 7-14 | 总体架构、微服务模块、中间件、数据库、核心流程、服务边界、搜索缓存 |
| 第四章：前端展示与系统演示 | 15-18 | 前端设计、用户端功能、B 端功能、演示流程 |
| 第五章：测试总结与展望 | 19-20 | 测试运行结果、项目亮点、总结展望 |

## 5. 详细页面计划

### 第 1 页：封面

- 标题：Omni 万象抢票平台
- 副标题：基于 Spring Cloud Alibaba 与 Next.js 的微服务票务系统设计与实现
- 负责人：全组

排版建议：

| 区域 | 内容 |
|---|---|
| 左上 | 学校 / 课程 / 小组信息 |
| 中央 | 大标题“Omni 万象抢票平台” |
| 标题下方 | 项目副标题 |
| 底部 | 小组成员姓名、日期 |
| 背景 | 演唱会舞台、霓虹灯、人群剪影、电子票券元素 |

生图提示词：

```text
16:9 横版答辩 PPT 封面，主题为“Omni 万象抢票平台”，赛博朋克风格演唱会现场，霓虹灯、舞台灯光、人群剪影、电子票券、城市夜景，主色为玫红色 #ff1268 和深蓝紫色，科技感强，留出中间大标题区域，中文标题“Omni 万象抢票平台”，副标题“基于 Spring Cloud Alibaba 与 Next.js 的微服务票务系统设计与实现”，高校课程答辩风格，清晰、专业、视觉冲击强
```

### 第 2 页：团队分工

- 标题：团队分工
- 负责人：成员 A

排版建议：三组并列卡片。

| 小组 | 成员 | 负责方向 | 技术关键词 |
|---|---|---|---|
| 第 1 组 | A、B | 业务需求与数据设计 | 需求分析、PostgreSQL、Redis |
| 第 2 组 | C、D | 后端微服务与中间件 | Spring Cloud、Nacos、OpenFeign、Sentinel、Seata |
| 第 3 组 | E、F | 前端展示与系统验证 | Next.js、ES、MQ |

关系图：

```text
A/B：业务需求与数据设计 → C/D：后端微服务与中间件 → E/F：前端展示与系统验证
```

生图提示词：

```text
16:9 PPT 页面，主题“团队分工”，现代科技风，深色背景，三张并列卡片，每张卡片代表一个小组：业务需求与数据设计、后端微服务与中间件、前端展示与系统验证。卡片之间用发光箭头连接，主色玫红 #ff1268，辅色蓝紫色，适合高校软件项目答辩，布局清晰，留出中文文字区域
```

### 第 3 页：项目背景

- 标题：项目背景
- 负责人：成员 A

核心内容：

| 背景问题 | 说明 |
|---|---|
| 热门票务需求集中 | 演唱会、赛事、展览等活动购票高峰明显 |
| 用户体验要求高 | 用户希望快速搜索、下单、支付、查看订单 |
| 系统并发压力大 | 热门活动开票时会产生瞬时高流量 |
| 管理角色复杂 | 平台管理员、主办方、普通用户职责不同 |

关系图：

```text
热门活动
├─ 高并发访问
├─ 库存变化快
├─ 支付链路复杂
└─ 多角色管理
```

生图提示词：

```text
16:9 PPT 背景图，票务平台项目背景，演唱会门票、手机购票页面、排队抢票、人群、服务器压力图标，深色科技背景，玫红和蓝紫霓虹色，左侧留出四个痛点文字卡片区域，右侧为抢票场景插画，专业答辩风格
```

### 第 4 页：项目目标

- 标题：项目目标
- 负责人：成员 A

核心内容：实现覆盖 C 端购票、B 端管理、平台后台管理的综合票务平台。

| 目标 | 内容 |
|---|---|
| 用户购票 | 浏览活动、查看详情、选择票档、创建订单、支付 |
| 主办方管理 | 发布活动、管理场次、管理票档、查看销售情况 |
| 平台管理 | 用户管理、活动审核、系统维护 |
| 技术实践 | 微服务拆分、服务治理、缓存、异步消息、搜索优化 |

关系图：

```text
            用户购票
               ↑
平台管理 ← Omni 平台 → 主办方管理
               ↓
            技术实践
```

生图提示词：

```text
16:9 PPT 页面，中心辐射图，中心为“Omni 万象抢票平台”，四周是“用户购票”“主办方管理”“平台管理”“技术实践”四个目标模块，深色科技背景，玫红 #ff1268 发光线条连接，现代互联网产品答辩风格，布局清晰
```

### 第 5 页：需求分析

- 标题：需求分析
- 负责人：成员 B

核心内容：

| 角色 | 核心需求 |
|---|---|
| 普通用户 | 注册登录、浏览活动、搜索活动、下单购票、支付、查看订单 |
| 主办方 | 活动发布、场次管理、票档管理、销售查看 |
| 管理员 | 用户管理、活动管理、主办方管理、平台数据维护 |

关系图：

```text
普通用户 → 浏览活动 / 下单 / 支付 / 查订单
主办方   → 发布活动 / 管理场次 / 管理票档
管理员   → 用户管理 / 活动管理 / 平台维护
```

生图提示词：

```text
16:9 PPT 页面，需求分析，三列角色卡片布局，分别为普通用户、主办方、管理员，每列包含对应功能图标，现代扁平科技风，深色背景，玫红和蓝紫配色，适合软件工程答辩，文字区域清晰
```

### 第 6 页：核心业务流程

- 标题：核心业务流程
- 负责人：成员 B

核心流程：

```text
登录 → 浏览活动 → 查看详情 → 选择票档/座位 → 创建订单 → 支付 → 查看订单
```

排版建议：横向时间轴流程图。

生图提示词：

```text
16:9 PPT 页面，核心业务流程图，展示票务平台用户购票流程：登录、浏览活动、活动详情、选择票档、创建订单、支付、查看订单。使用横向时间轴或箭头流程，配手机购票、门票、支付、订单图标，深色科技风，玫红 #ff1268 高亮关键节点，清晰专业
```

### 第 7 页：系统总体架构

- 标题：系统总体架构
- 负责人：成员 C

技术栈：

| 层级 | 技术 |
|---|---|
| 前端 | Next.js 16 + React 19 |
| 网关 | Spring Cloud Gateway |
| 注册中心 | Nacos |
| 后端服务 | user、ticket、order、payment、notification |
| 数据库 | PostgreSQL 五库拆分 |
| 中间件 | Redis、MQ、ES、Sentinel、Seata |

关系图：

```text
用户浏览器
   ↓
Next.js 前端
   ↓
Spring Cloud Gateway
   ↓
Nacos 服务发现
   ↓
user / ticket / order / payment / notification
   ↓
PostgreSQL 五库 + Redis + MQ + ES
```

生图提示词：

```text
16:9 PPT 系统总体架构图，前后端分离微服务架构，顶部为用户浏览器和 Next.js 前端，中间为 Spring Cloud Gateway 和 Nacos，下面是 user、ticket、order、payment、notification 五个微服务，底部是 PostgreSQL 五库、Redis、MQ、ES。深色科技背景，节点卡片和发光连线，玫红 #ff1268 作为主色，结构清晰，适合软件项目答辩
```

### 第 8 页：微服务模块划分

- 标题：微服务模块划分
- 负责人：成员 C

核心内容：

| 服务 | 职责 |
|---|---|
| java-user | 用户、登录、角色管理 |
| java-ticket | 活动、场次、票档、库存、座位 |
| java-order | 订单创建、订单快照、订单状态 |
| java-payment | 支付二维码、支付同步、退款校验 |
| java-notification | 通知消息、订单通知 |
| java-gateway | 统一入口、路由转发 |

关系图：

```text
前端 → Gateway → java-user / java-ticket / java-order / java-payment / java-notification
```

生图提示词：

```text
16:9 PPT 页面，微服务模块划分图，中心是 Spring Cloud Gateway，周围连接 java-user、java-ticket、java-order、java-payment、java-notification 五个业务服务，每个服务用独立卡片表示并带简短职责图标，深色科技风，玫红蓝紫配色，清晰展示服务拆分
```

### 第 9 页：微服务治理与服务调用

- 标题：微服务治理与服务调用
- 负责人：成员 C、成员 D

核心内容：

| 技术 | 作用 |
|---|---|
| Spring Cloud | 微服务基础框架 |
| Nacos | 服务注册与发现 |
| OpenFeign | 服务间 HTTP 调用 |
| Sentinel | 限流、熔断、降级 |

关系图：

```text
服务启动 → 注册到 Nacos
前端请求 → Gateway
Gateway → 根据服务名转发
order 服务 → OpenFeign 调用 user / ticket
Sentinel → 保护高并发接口
```

生图提示词：

```text
16:9 PPT 页面，微服务治理与服务调用关系图，展示 Spring Cloud、Nacos、OpenFeign、Sentinel 的作用。包含 Gateway、Nacos 注册中心、order 服务调用 user 服务和 ticket 服务的箭头关系，Sentinel 作为限流熔断保护层。深色背景，科技风节点图，玫红 #ff1268 高亮调用链路，专业答辩风格
```

### 第 10 页：高并发与数据一致性设计

- 标题：高并发与数据一致性设计
- 负责人：成员 B、成员 D、成员 F

核心内容：

| 技术 | 使用场景 |
|---|---|
| Redis | 活动缓存、库存缓存、抢票锁、防重复提交 |
| Sentinel | 热门接口限流、熔断降级 |
| Seata | 分布式事务一致性设计 |
| MQ | 异步通知、削峰解耦 |

关系图：

```text
抢票请求高峰
├─ Redis：缓存库存，降低数据库压力
├─ Sentinel：限流保护服务
├─ Seata：保证跨服务事务一致性
└─ MQ：异步通知，削峰解耦
```

生图提示词：

```text
16:9 PPT 页面，高并发与数据一致性设计，中心是“抢票高峰”，周围四个技术模块：Redis 缓存库存、Sentinel 限流熔断、Seata 分布式事务、MQ 异步削峰。用发光箭头连接，背景为演唱会开票高并发场景和服务器压力图，深色科技风，玫红蓝紫配色
```

### 第 11 页：数据库设计

- 标题：数据库设计
- 负责人：成员 B

五库拆分：

| 服务 | 数据库 |
|---|---|
| java-user | omni_user |
| java-ticket | omni_ticket_split |
| java-order | omni_order |
| java-payment | omni_payment |
| java-notification | omni_notification |

设计原则：

```text
服务拥有自己的数据库
禁止跨服务直接查表
订单通过 order_snapshot 保存快照
服务之间通过 internal API 协作
```

关系图：

```text
java-user         → omni_user
java-ticket       → omni_ticket_split
java-order        → omni_order
java-payment      → omni_payment
java-notification → omni_notification
```

生图提示词：

```text
16:9 PPT 页面，数据库五库拆分设计图，展示 java-user 对应 omni_user，java-ticket 对应 omni_ticket_split，java-order 对应 omni_order，java-payment 对应 omni_payment，java-notification 对应 omni_notification。每个服务和数据库用一组卡片连接，强调“服务独立拥有数据库，禁止跨服务直接查表”，深色科技风，玫红和蓝紫配色
```

### 第 12 页：核心流程：下单与支付

- 标题：核心流程：下单与支付
- 负责人：成员 D

核心链路：

```text
用户提交订单
→ order 服务校验用户
→ ticket 服务锁定库存 / 座位
→ order 服务创建订单快照
→ payment 服务生成支付二维码
→ 支付完成
→ order 服务更新订单状态
→ ticket 服务确认售出
```

泳道图：

```text
用户 → Gateway → order
order → user：校验用户
order → ticket：锁定库存
order：创建订单
order → payment：发起支付
payment → order：同步支付状态
order → ticket：确认售出
order → notification：发送通知
```

生图提示词：

```text
16:9 PPT 页面，下单与支付核心流程泳道图，泳道包括用户、Gateway、order 服务、user 服务、ticket 服务、payment 服务。展示提交订单、校验用户、锁定库存、创建订单、生成支付二维码、支付成功、更新订单状态、确认售出的完整流程。深色科技风，箭头清晰，关键节点用玫红 #ff1268 高亮
```

### 第 13 页：服务间调用与边界控制

- 标题：服务间调用与边界控制
- 负责人：成员 D

边界规则：

| 边界规则 | 说明 |
|---|---|
| order 不直接查 user/ticket 表 | 通过 internal API 调用 |
| payment 不直接查 order/user/ticket 表 | 通过 order/user/ticket 服务协作 |
| notification 只保存 copied id | 不拥有用户和订单数据 |
| internal API 必须校验 token | 使用 `X-Internal-Token` |

对比图：

```text
错误方式：
order 服务 ──SQL──→ user 表 / ticket 表

正确方式：
order 服务 ──internal API──→ user 服务 / ticket 服务
```

生图提示词：

```text
16:9 PPT 页面，服务边界控制对比图，左侧显示错误方式：order 服务直接 SQL 查询 user 表和 ticket 表，用红色叉号标注；右侧显示正确方式：order 服务通过 internal API 调用 user 服务和 ticket 服务，用绿色或玫红高亮。深色科技风，适合微服务架构答辩，文字区域清晰
```

### 第 14 页：搜索与缓存设计

- 标题：搜索与缓存设计
- 负责人：成员 B、成员 E

核心内容：

| 技术 | 作用 |
|---|---|
| Redis | 缓存活动详情、票档库存、热点数据 |
| ES | 活动标题、地点、分类、主办方等全文检索 |
| PostgreSQL | 保存核心业务数据 |

关系图：

```text
活动浏览 → Redis 缓存 → PostgreSQL
活动搜索 → ES 检索 → 活动结果
```

生图提示词：

```text
16:9 PPT 页面，搜索与缓存设计图，展示用户浏览活动时访问 Redis 缓存，未命中访问 PostgreSQL；用户搜索活动时访问 ES 全文检索，返回活动列表。包含手机搜索框、活动卡片、Redis、ES、PostgreSQL 图标，深色科技风，玫红蓝紫配色，清晰专业
```

### 第 15 页：前端页面设计

- 标题：前端页面设计
- 负责人：成员 E

核心内容：

| 页面类型 | 页面 |
|---|---|
| C 端 | 登录、活动列表、活动详情、订单列表 |
| B 端 | 管理控制台、活动管理、订单管理、用户管理 |
| 公共能力 | 请求封装、认证状态、路由跳转 |

关系图：

```text
Next.js 前端
├─ C 端用户页面
├─ B 端管理页面
├─ request<T>() 请求封装
└─ auth.ts 登录状态管理
```

生图提示词：

```text
16:9 PPT 页面，前端页面设计展示，包含多个浏览器窗口样式的页面 mockup：登录页、活动列表页、活动详情页、订单页、管理控制台。整体为现代票务平台 UI，品牌色 #ff1268，白色内容卡片搭配深色科技背景，适合项目答辩展示
```

### 第 16 页：用户端功能展示

- 标题：用户端功能展示
- 负责人：成员 E

核心内容：

| 功能 | 说明 |
|---|---|
| 用户登录 | 手机号和密码登录 |
| 活动浏览 | 查看演唱会、展览等活动 |
| 活动详情 | 查看场次、票档、价格 |
| 下单支付 | 创建订单并完成支付 |
| 订单查看 | 查看订单状态 |

用户路径：

```text
登录 → 活动列表 → 活动详情 → 支付 → 我的订单
```

生图提示词：

```text
16:9 PPT 页面，用户端功能展示，五个手机界面卡片横向排列，分别展示登录、活动列表、活动详情、支付、我的订单。界面风格类似现代票务平台，主色玫红 #ff1268，清爽白色卡片，背景为深色霓虹舞台，适合答辩展示
```

### 第 17 页：B 端管理功能展示

- 标题：B 端管理功能展示
- 负责人：成员 E

核心内容：

| 角色 | 功能 |
|---|---|
| 主办方 | 发布活动、管理场次、管理票档 |
| 管理员 | 用户管理、活动管理、主办方管理 |
| 平台 | 数据维护、订单查看、运营支持 |

功能结构：

```text
B 端控制台
├─ 活动管理
├─ 场次管理
├─ 票档管理
├─ 用户管理
└─ 订单管理
```

生图提示词：

```text
16:9 PPT 页面，B 端管理功能展示，展示一个现代后台管理控制台界面，左侧深色侧边栏，右侧为活动管理、场次管理、票档管理、用户管理、订单管理的数据表格和卡片。品牌色 #ff1268，界面专业清晰，适合软件系统答辩
```

### 第 18 页：系统演示流程

- 标题：系统演示流程
- 负责人：成员 F

演示顺序：

```text
1. 用户登录
2. 浏览活动列表
3. 查看活动详情
4. 选择票档 / 座位
5. 创建订单
6. 发起支付
7. 查看订单状态
8. 切换 B 端查看管理功能
```

演示流程图：

```text
C 端购票演示
↓
订单支付演示
↓
B 端管理演示
↓
测试结果展示
```

生图提示词：

```text
16:9 PPT 页面，系统演示流程图，展示软件答辩现场演示步骤：用户登录、浏览活动、查看详情、创建订单、支付、查看订单、B 端管理。使用时间轴布局，配电脑屏幕、手机、订单、支付图标，深色科技风，玫红高亮关键步骤
```

### 第 19 页：测试与运行结果

- 标题：测试与运行结果
- 负责人：成员 F

核心内容：

| 测试类型 | 验证内容 |
|---|---|
| 接口测试 | 登录、活动、订单、支付接口 |
| 前端检查 | `pnpm typecheck` |
| 微服务边界检查 | `verify-microservice-boundaries.ps1` |
| 运行验证 | Gateway、Nacos、五个后端服务、前端服务 |
| 数据库验证 | 五库连接正常，无业务服务误连共享库 |

验证链路：

```text
功能测试 → 接口测试 → 边界检查 → 运行验证 → 答辩演示通过
```

生图提示词：

```text
16:9 PPT 页面，测试与运行结果展示，深色科技背景，左侧为测试项目列表：接口测试、前端检查、微服务边界检查、运行验证、数据库验证；右侧为终端窗口和通过状态卡片，使用绿色通过标识和玫红重点高亮，专业软件答辩风格
```

### 第 20 页：项目亮点、总结与展望

- 标题：项目亮点与总结展望
- 负责人：成员 A、成员 F

项目亮点：

| 亮点 | 说明 |
|---|---|
| 微服务拆分清晰 | user、ticket、order、payment、notification 独立职责 |
| 服务边界明确 | 禁止跨服务查表，通过 internal API 协作 |
| 五库拆分 | 接近生产环境的物理拆库设计 |
| 订单快照 | 订单展示不依赖跨服务查询 |
| 中间件体系完整 | 覆盖注册发现、远程调用、缓存、限流、消息、搜索、事务 |
| 前后端完整闭环 | 覆盖用户购票和 B 端管理 |

不足与展望：

```text
继续完善高并发抢票策略
增强监控和日志分析能力
完善支付回调与异常补偿机制
提升活动搜索和推荐能力
优化前端交互体验
```

生图提示词：

```text
16:9 PPT 结束页，主题“项目亮点与总结展望”，上半部分展示六个项目亮点卡片：微服务拆分、服务边界、五库拆分、订单快照、中间件体系、前后端闭环；下半部分展示未来优化时间轴：高并发优化、监控告警、搜索推荐、用户体验提升。深色科技风，玫红 #ff1268 和蓝紫色，专业答辩总结页
```

## 6. 核心关系图清单

### 6.1 团队分工协作图

用途：第 2 页。

```text
A/B：业务需求与数据设计
↓
C/D：后端微服务与中间件
↓
E/F：前端展示与测试演示
```

### 6.2 系统总体架构图

用途：第 7 页。

```text
用户浏览器
↓
Next.js 前端
↓
Spring Cloud Gateway
↓
Nacos 服务发现
↓
user / ticket / order / payment / notification
↓
PostgreSQL 五库 + Redis + MQ + ES
```

### 6.3 微服务模块关系图

用途：第 8 页。

```text
Gateway
├─ java-user
├─ java-ticket
├─ java-order
├─ java-payment
└─ java-notification
```

### 6.4 中间件关系图

用途：第 9-10 页。

```text
Spring Cloud：微服务基础
Nacos：服务注册发现
OpenFeign：服务间调用
Sentinel：限流熔断
Seata：分布式事务
Redis：缓存和库存
MQ：异步通知
ES：搜索检索
```

### 6.5 下单支付流程泳道图

用途：第 12 页。

```text
用户 → Gateway → order
order → user：校验用户
order → ticket：锁定库存
order：创建订单
order → payment：发起支付
payment → order：同步支付状态
order → ticket：确认售出
order → notification：发送通知
```

### 6.6 数据库五库拆分图

用途：第 11 页。

```text
java-user         → omni_user
java-ticket       → omni_ticket_split
java-order        → omni_order
java-payment      → omni_payment
java-notification → omni_notification
```

### 6.7 前端页面结构图

用途：第 15 页。

```text
Next.js 前端
├─ C 端用户页
│  ├─ 登录
│  ├─ 活动列表
│  ├─ 活动详情
│  └─ 订单页
└─ B 端控制台
   ├─ 活动管理
   ├─ 场次管理
   ├─ 票档管理
   └─ 用户管理
```

## 7. 图片生成计划汇总

| 页码 | 图片类型 | 优先级 | 说明 |
|---|---|---|---|
| 1 | 封面主视觉 | 必须 | 决定整体风格 |
| 2 | 团队分工图 | 建议 | 明确 6 人职责 |
| 3 | 项目背景插画 | 建议 | 展示票务痛点场景 |
| 4 | 项目目标辐射图 | 建议 | 展示系统目标 |
| 5 | 角色需求图 | 建议 | 用户、主办方、管理员 |
| 6 | 购票业务流程图 | 必须 | 答辩核心业务 |
| 7 | 系统总体架构图 | 必须 | 技术答辩核心 |
| 8 | 微服务模块图 | 必须 | 展示后端拆分 |
| 9 | 服务治理图 | 必须 | 展示 Spring Cloud、Nacos、OpenFeign、Sentinel |
| 10 | 高并发中间件图 | 必须 | 展示 Redis、MQ、Seata、Sentinel |
| 11 | 数据库五库拆分图 | 必须 | 项目亮点 |
| 12 | 下单支付泳道图 | 必须 | 核心流程 |
| 13 | 服务边界对比图 | 建议 | 展示工程规范 |
| 14 | 搜索缓存图 | 建议 | 展示 Redis、ES |
| 15 | 前端页面结构图 | 建议 | 展示前端架构 |
| 16 | C 端功能界面图 | 建议 | 可用真实截图替代 |
| 17 | B 端管理界面图 | 建议 | 可用真实截图替代 |
| 18 | 演示流程图 | 建议 | 答辩现场辅助 |
| 19 | 测试结果图 | 建议 | 可用终端截图替代 |
| 20 | 总结展望图 | 建议 | 收尾提升完整度 |

## 8. 答辩发言分配

| 顺序 | 发言人 | 对应页码 | 时间 | 内容 |
|---|---|---|---|---|
| 1 | 成员 A | 1-4 | 1.5 分钟 | 开场、团队分工、背景、目标 |
| 2 | 成员 B | 5-6、11、14 | 2 分钟 | 需求、业务流程、数据库、Redis 缓存 |
| 3 | 成员 C | 7-9 | 2 分钟 | 总体架构、微服务拆分、Spring Cloud、Nacos |
| 4 | 成员 D | 9-13 | 2.5 分钟 | OpenFeign、Sentinel、Seata、下单支付、边界控制 |
| 5 | 成员 E | 14-17 | 1.5 分钟 | ES 搜索、前端页面、用户端和 B 端展示 |
| 6 | 成员 F | 18-20 | 2 分钟 | 系统演示、MQ、测试、总结展望 |

## 9. 推荐制作顺序

建议不要按页码直接制作，而是先保证核心技术页质量。

1. 第 7 页：系统总体架构图
2. 第 12 页：下单与支付流程图
3. 第 11 页：数据库五库拆分图
4. 第 9-10 页：中间件设计图
5. 第 1 页：封面
6. 第 15-17 页：前端展示页
7. 第 2-6 页和第 18-20 页

## 10. 给 PPT 生成 AI 的执行建议

生成 PPT 时建议遵循以下原则：

1. 每页只突出一个主题，不要堆叠过多文字。
2. 架构图、流程图、关系图优先使用清晰箭头和分层卡片。
3. 技术名词保持英文原文，例如 Spring Cloud、Nacos、OpenFeign、Sentinel、Seata、Redis、MQ、ES。
4. 用户可见标题和说明使用中文。
5. 页面风格统一使用深色科技背景和 `#ff1268` 品牌色。
6. 第 7、11、12 页是技术核心页，需要重点打磨。
7. 第 16、17、19 页如果有真实系统截图，优先使用真实截图替代 AI 生成界面。
