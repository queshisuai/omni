# 生产前种子数据审计清单

> 阶段 0 输出物。当前本地数据库主要是种子数据，本文用于标记哪些数据可保留、哪些需要调整、哪些需要迁移配合。本文不直接写入本地数据库。

## 审计结论

项目已经有一套较完整的 prod-split real-demo seed，覆盖面优于早期共享库 `sql/seed.sql`。后续生产前演示和验收应以 `sql/seeds/prod-split-real-demo` 为主，历史共享库 seed 只保留兼容说明，避免误用。

2026-06-08 已补齐平台主办方运营员 seed：基线中文名和演示账号昵称统一为“平台主办方运营员”，`04-user-ops.sql` 增加主办方分配、风险等级、下次跟进、跟进记录和运营审计，并已同步本地 `omni_user`。

2026-06-08 已补齐客服上下文工作台 seed：`04-user-ops.sql` 增加客服账号、`support_account`、会话、消息、备注、标签、审计和快捷回复；会话指向 `DMREAL980045` 与 `REFREAL985004`，可联动现有订单、退款、票夹、候补、抢票和通知演示数据。已按授权执行 `scripts\apply-prod-split-real-demo-seed.ps1 -ConfirmApply` 持久写入本地五库，`scripts\verify-prod-split-real-demo-seed.ps1` 通过，并完成真实客服会话数据面板浏览器验收。

2026-06-08 已补齐评价系统正式化 seed：`01-ticket.sql` 增加已展示、待审核、已隐藏评价，待回复、已回复、已隐藏问答，以及待处理/已处理举报样本；新增片段已同步本地 `omni_ticket_split`，`scripts\verify-prod-split-real-demo-seed.ps1` 通过。

2026-06-09 已补齐入场核验第一阶段 seed：`02-order.sql` 增加启用/停用核验设备、成功核验、重复扫码和停用设备失败记录，并调整电子票状态覆盖未入场、已验票、已失效、已转赠。标准验收样本为 `sessionId=910011`，平台管理员和主办方均可在 `/console/check-in` 查看概览和三条核验记录。

2026-06-09 阶段 9 全角色旅程审计发现两个 seed 缺口：主办方退款处理入口可达但当前列表为空；普通客服默认待处理队列为空，需要切到已关闭会话才能看到用户上下文。已补 `REFREAL985009` 主办方退款样本和 `988102` 普通客服默认待处理样本，已重新执行本地导入、verifier、DB/API 和浏览器复测。

2026-06-10 已补齐 BY2、胡夏两个重点艺人档案的本地头像归档：新增 `artist-avatars.json` 和 `frontend/public/seed-artist-avatars-real/*`，`01-ticket.sql` 不再把这两个艺人头像指向活动海报，verifier 已纳入头像清单、来源链接和本地文件检查。

## 可保留资产

| 资产 | 价值 |
|:---|:---|
| `sql/seeds/prod-split-real-demo/README.md` | 明确声明仅用于本地 prod-split 联调，不属于生产迁移链路。 |
| `01-ticket.sql` | 覆盖 12 城市 x 10 分类，共 120 条活动，适合搜索和首页演示。 |
| `artist-avatars.json` | 记录重点艺人头像来源页、本地归档路径和文件大小，避免 seed 依赖远端图或复用活动海报。 |
| `02-order.sql` | 覆盖订单、订单快照、电子票，适合订单和票夹演示。 |
| `03-payment.sql` | 支撑支付和退款演示。 |
| `04-user-ops.sql` | 支撑用户、客服、RBAC、运营后台演示。 |
| `05-notification.sql` | 支撑通知中心演示。 |
| `06-grab.sql` | 支撑抢票、候补、小队和失败原因演示。 |

## 需要调整或补齐

| 问题 | 证据 | 建议 |
|:---|:---|:---|
| 历史 `sql/seed.sql` 容易被误用 | 仍包含共享库式初始化、模拟活动、`review` 清理等历史内容 | 在文档中标明“历史共享库 seed”，后续演示只使用 real-demo seed。 |
| 评价/问答数据不足 | 第一轮已补 real-demo 评价、问答和举报状态样本，并同步本地 `omni_ticket_split` | 后续按审核统计、违规原因、主办方回复效率继续扩展样本。 |
| 入场核验异常补录演示不足 | 第一阶段已补未入场、已验票、无效、转赠后票据、成功核验、重复扫码和设备停用失败；仍缺过期码失败、退款后拒绝、线下同步失败补录、工作人员权限样本 | 后续如果做异常补录或备用扫码页，再补对应 seed 和 verifier。 |
| 主办方退款列表演示数据不足 | 已补 `REFREAL985009`，关联 `980006` / `910006` / `900006` / 主办方 `2003`，主办方 `GET /api/payment/refunds/admin` 返回 1 条 | 已关闭；保留该样本作为 `/console/refunds` 标准演示数据。 |
| 普通客服默认队列演示数据不足 | 已补 `988102`，状态 `WAITING_AGENT`、未分配、`slaOverdue=false`，`/support` 默认显示“待处理 1” | 已关闭；保留该样本作为普通客服默认工作台标准演示数据。 |
| 候补/小队页面展示 ID | waitlist/team 前端仍有活动、场次、票档 ID 展示 | seed 可保留 ID，但接口或前端需要补充名称字段。 |
| 平台主办方运营员 role code 仍是兼容命名 | 内部 role code 暂保留 `organizer_admin`；中文名称和 seed 昵称已统一为“平台主办方运营员” | 第一轮不改 role code；若后续改成 `organizer_ops_agent` / `organizer_ops_manager`，需要新增兼容迁移和 seed 同步调整。 |
| 平台主办方运营员二期演示数据不足 | real-demo seed 已有分配、风险等级、下次跟进、跟进历史和运营审计；还缺双人复核、自动分配负载和跨服务业务摘要样本 | 后续增加待复核动作、运营员负载、主办方活动/订单/退款摘要，支撑二期工作台演示。 |
| 通知场景不够统一 | 目前通知 seed 已有站内通知，但通知事件中台还未统一 | 后续按通知事件模型补 `aggregateKey`、模板、渠道状态等数据。 |

## 数据库变更纪律

- 只改种子数据，不等于完成结构变更。
- 新增表、字段、索引、约束必须写到对应 `sql/production-split/{service}` 目录。
- 结构迁移写完后，需要执行本地数据库迁移，再验证代码。
- 种子数据必须和当前五库拓扑一致，不写入旧 `omni_ticket` 作为默认业务库。
- 涉及用户、订单、票务跨服务数据时，只保存 copied id 或快照，不新增跨库外键。

## 建议补齐的演示用户旅程

| 角色 | 数据要求 |
|:---|:---|
| 普通用户 | 浏览、搜索、详情、购票、支付、退款、票夹、转赠、候补、抢票、评价、客服、通知。 |
| 主办方 | 活动草稿、场次、票档、座位图、风险整改、退款处理、营销概览。 |
| 客服 | 待接入、处理中、超时、升级、关闭确认、带订单和退款上下文的会话。 |
| 客服主管 | 客服账号、会话质检、SLA、队列压力。 |
| 平台主办方运营员 | 入驻审核、主办方跟进、风险待办、运营备注、运营员分配、下次跟进、待复核动作。 |
| 平台管理员 | RBAC、审计、异常任务、日结对账、恢复售票审核、评价审核。 |

## 下一步建议

1. 如果评价系统后续增加审核统计或违规标签，先写迁移。
2. 如果主办方运营员重命名涉及角色码，先设计兼容迁移，再更新 seed。
3. 阶段 9 已用 `REFREAL985009`、`988102` 和 `REAL-DEMO-20260603` 完成第一批可回滚写动作验收；后续新增写动作样本仍需先设计 re-seed 恢复路径，避免污染当前 real-demo 基线。
