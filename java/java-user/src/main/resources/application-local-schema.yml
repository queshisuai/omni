spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/omni_ticket?currentSchema=user_service,public

omni:
  privacy:
    id-no-key: ${OMNI_ID_NO_KEY:omni-local-dev-id-no-key-change-me}
