# 大麦迁移体验改进执行计划

## 当前状态

总路线中的本地生产前收口项已完成，已从本文件的待办列表移除。

历史执行过程继续保留在 `progress.md`，关键技术发现继续保留在 `findings.md`。后续如果新增范围，需要重新开明确任务、验收标准和验证命令。

当前继续按二期增强候选做小切片推进。阶段 11 第五轮“搜索页右栏推荐吸收近期浏览信号”、第六轮“搜索空结果最近浏览召回”、第七轮“搜索联想吸收近期浏览标题和艺人”、第八轮“订阅页空态承接近期浏览并回到详情开启提醒/关注”、第九轮“候补上下文缺失时不再以场次/票档 ID 兜底”、第十轮“小队房间隐藏 requestId/用户 ID/seatId 等技术标识”，阶段 12 第一轮“退款审核页 CSV 明细导出”、第二轮“入场核验记录 CSV 导出”、第三轮“日结对账单 CSV 导出”，以及阶段 13 第一轮“入场核验页场次查询文案去技术 ID 化”、第二轮“审计日志筛选与目标引用编号中文化”、第三轮“对账页后端码值统一中文兜底”、第四轮“站点变更类型未知值中文兜底”、第五轮“异常任务未知码值中文兜底”、第六轮“操作审计未知枚举中文兜底”、第七轮“对账摘要未知字段中文兜底”、第八轮“退款审核页编号语境中文化”、第九轮“控制台首页最近对账状态中文兜底”、第十轮“平台主办方运营页编号语境中文化”、第十一轮“审计日志列表操作人编号语境中文化”、第十二轮“场次列表活动/场馆编号语境中文化”、第十三轮“恢复售票/场馆座位图井号编号语境中文化”、第十四轮“评价问答管理页编号语境中文化”、第十五轮“控制台详情路由错误编号语境中文化”和第十六轮“风控案例/客服会话编号语境中文化”已完成并记录到 `progress.md` / `findings.md`。阶段 12 第十六轮“场次页票档批量导入入口”和第十七轮“票档创建/更新操作审计”也已完成并通过本地验证。后续可继续补阶段 12/13/14 的下一个本地可验证切片。

阶段 14 第一轮已补齐 `scripts/measure-gateway-latency.ps1` 的结构化留证输出：新增 `scenario` 字段，并支持 `-OutputFormat Object|Csv|Json` 与 `-OutputPath`，用于把 Gateway/direct 与 local-model 耗时样本导出成可归档文件。

阶段 14 第二轮已补齐小队抢票处理器分段耗时日志：`TeamGrabProcessorService` 输出“小队抢票链路耗时”，区分锁座、票价读取、建单、确认落库和通知成员耗时。

阶段 14 第三轮已补齐支付确认分段耗时日志：`PaymentConfirmationService` 输出“支付确认链路耗时”，区分订单确认/出票履约调用与本地支付流水更新耗时。

阶段 14 第四轮已补齐支付同步外层分段耗时日志：`AlipayService.syncByOrderId()` 输出“支付同步链路耗时”，区分订单读取、支付流水读取、支付宝查询、本地确认和订单回查耗时。

阶段 14 第五轮已补齐订单支付履约分段耗时日志：`OrderService.markPaid()` 输出“订单支付履约链路耗时”，区分订单读取、状态更新、票务确认、电子票出票和候补通知耗时。

阶段 14 第六轮已补齐电子票出票分段耗时日志：`TicketWalletService.issueForPaidOrder()` 输出“电子票出票链路耗时”，区分幂等检查、观演人读取、座位读取和电子票写入耗时。

阶段 14 第七轮已补齐 AI 客服回复分段耗时日志：`SupportAiService.answerStreamingWithDiagnostics()` 输出“AI客服回复链路耗时”，记录 FAQ / local-model / default 来源、首字耗时、总耗时和模型回落原因。

阶段 12 第四轮已补齐退款审核页 Excel 明细导出：在既有 CSV 字段基础上新增浏览器可打开的 `.xls` HTML 表格导出，继续只导出退款号、订单号、活动、金额、状态、原因和时间线，不导出 `userId`、`paymentId`、`reviewerId`、`alipayRefundNo`、内部 `orderId` 等敏感或内部追踪字段。

阶段 12 第五轮已补齐入场核验记录 Excel 明细导出：在既有 CSV 字段基础上新增 `.xls` HTML 表格导出，继续只导出请求号、票号、设备、渠道、结果、失败原因和核验时间，不导出 `ticketId`、`orderId`、`userId`、`sessionId`、`ticketTypeId`、`operatorUserId` 等内部关联编号。

阶段 12 第六轮已补齐日结对账单 Excel 明细导出：在既有 CSV 字段基础上新增 `.xls` HTML 表格导出，复用对账中文 formatter，继续只导出批次号、业务日期、来源、业务号、业务类型/差异类型、金额、状态、原因和生成时间，不导出行级 `id` 或 `local`、`processing`、`matched`、`amount_mismatch`、`open` 等后端码值。

阶段 12 第七轮已补齐场次维度报表导出：`/console/sessions` 当前页可导出 CSV 与 Excel，字段限定为活动、场馆、城市、开始/结束时间、状态、票档数、总库存、已售和余票，不导出场次行级 `id`、`activityId`、`venueId` 等内部字段名。

阶段 13 第十七轮已补齐前端 API 参数校验错误文案编号化：`assertPositiveInteger()` 统一把中文参数标签里的 `ID` 归一为“编号”，让活动、场次、用户和 SeatCraft 归属等前端拦截错误不再显示“活动ID不正确 / 场次ID不正确 / 用户ID不正确”。

阶段 13 第十八轮已补齐艺人编辑/场馆列表编号语境中文化：艺人编辑页动态路由错误改为“艺人编号不正确”，`/console/venue` 列表表头从裸 `ID` 改为“场馆编号”，继续只改用户可见文案，不改路由参数、接口字段或表格中的实际编号值。

阶段 11 第十一轮已补齐活动详情加入小队编号文案：`/activity/[id]` 加入已有小队弹窗从“小队 ID”改为“小队编号”，校验提示同步改为“小队编号必须是正整数”，不改 `teamId`、邀请码或加入小队接口逻辑。

阶段 13 第十九轮已补齐客服工作台编号语境中文化：`/support` 会话头部从 `ID：{userId}` 改为“用户编号：{userId}”，抢票上下文从“抢票 {requestId}”改为“抢票请求号：{requestId}”，保留客服排查所需编号但不再裸露技术口吻。

阶段 13 第二十轮已补齐商户入驻页编号语境中文化：`/merchant` 申请说明从“用户 ID”改为“用户编号”，保持后端自动识别当前登录账号的说明不变。

阶段 11 第十二轮已补齐 C 端票档缺名兜底去内部编号：订单列表、订单详情、票夹、活动详情抢票尝试列表和自动降档提示在票档名缺失时统一显示“票档信息待同步”，不再用 `ticketTypeId` 拼“票档 123”这类内部编号文案。

阶段 11 第十三轮已补齐活动详情评价作者去用户编号化：活动详情“评价与问答”中的观演评价作者从“用户 {userId}”改为“匿名用户”，避免 C 端把内部用户编号作为评论者身份展示。

阶段 11 第十四轮已补齐浏览记录标题兜底去活动编号化：`/history` 最近浏览标题缺失时显示“演出信息待同步”，不再用 `activityId` 拼“演出 900001”这类内部编号文案。

阶段 13 第二十一轮已补齐控制台订单票档缺名兜底去内部编号：后台订单列表和 CSV 导出在票档名缺失时统一显示“票档信息待同步”，不再用 `ticketTypeId` 拼“票档 123”这类内部编号文案。

阶段 13 第二十二轮已补齐客服会话用户 fallback 编号语境中文化：`/support` 和 `/console/support-conversations` 在昵称、手机号缺失时显示“用户编号：{userId}”，不再以“用户 123”这类裸数字作为会话用户名称。

阶段 13 第二十三轮已补齐客服用户上下文卡片兜底中文化：`/support` 右侧用户上下文中的订单、退款、票券、候补、通知缺少业务号或标题时统一显示“对象编号：{id}”，抢票和候补状态缺少中文解释时显示固定中文待同步文案，不再回显原始状态码。

阶段 13 第二十四轮已补齐巡演与评价问答未知状态中文兜底：`/console/activity-engagement` 的未知问答/举报状态不再原样回显后端码值，`/console/tours/[id]` 的未知发布/配置状态统一显示中文兜底文案，避免后台用户直接看到未映射枚举。

阶段 13 第二十五轮已补齐恢复售票审核未知状态中文兜底：`/console/risk-resolutions` 对未知审核状态显示“未知审核状态”，不再把 `item.status` 原样渲染到恢复售票审核记录中。

阶段 13 第二十六轮已补齐风险事件待办未知恢复状态可见兜底：`/console/risk-events` 在最新恢复申请存在但状态未映射时显示“未知审核状态”，不再把状态徽标直接隐藏。

阶段 13 第二十七轮已补齐控制台订单降档路径编号语境中文化：`/console/orders` 在展示自动降档的原票档和实际票档编号时改为“原票档编号 / 实际票档编号”语境，不再使用裸 `#123 -> #456` 形式。

阶段 13 第二十八轮已补齐场馆资料审核未知状态中文兜底：`/console/venue/apply` 和 `/console/venue/applications` 对未知审核状态显示“未知场馆审核状态”，不再直接用状态映射表裸取导致空白。

阶段 13 第二十九轮已补齐风险案例未知恢复状态可见兜底：`/console/risk-cases` 在最新恢复状态未映射时显示“未知审核状态”，不再把状态徽标静默隐藏。

阶段 11 第十五轮已补齐票夹未知状态不误标已失效：`/tickets` 对未知电子票状态的徽标显示“状态同步中”，与票夹说明文案保持一致，不再复用“已失效”状态兜底。

阶段 13 第三十轮已补齐入场核验未知结果中文兜底：`/console/check-in` 对未知核验结果显示“未知结果”，与 CSV/Excel 导出层保持一致，不再原样回显后端结果码。

阶段 11 第十六轮已补齐订单列表未知状态不误标已取消：`/orders` 对未知订单状态显示“订单状态更新中”，与订单详情说明保持一致，不再复用“已取消”状态兜底。

阶段 13 第三十一轮已补齐通知中心未知类型中文兜底：通知徽标对未知 `type` 显示“未知消息”，不再误标为“站内消息”，并保留原始 key 供后续动作路由判断。

阶段 13 第三十二轮已补齐控制台订单未知状态中文兜底：`/console/orders` 页面和 CSV 导出统一通过 `formatConsoleOrderStatusLabel()` 显示“未知订单状态”，不再用空横杠掩盖后端新状态。

阶段 11 第十七轮已补齐小队成员未知状态中文兜底：`TeamMemberList` 对未知成员状态显示“状态同步中”，避免后端新增状态时房间成员列表状态空白。

阶段 11 第十八轮已补齐小队策略和房间状态未知兜底：`strategyLabel()` 未知策略显示“未知策略”，`teamStatusLabel()` 未知房间状态显示“状态同步中”。

阶段 11 第十九轮已补齐小队抢票进度未知状态兜底：`/teams/[id]` 抢票进度状态统一通过 `formatGrabProgressStatus()` 展示，未知进度状态显示“状态同步中”，不再因 `GRAB_STATUS_LABELS[progress.status]` 未命中而空白。

阶段 11 第二十轮已补齐活动详情抢票主状态未知兜底：`/activity/[id]` 抢票进度弹窗主状态统一通过 `formatGrabStatusLabel()` 展示，未知状态显示“状态同步中”，不再显示泛化“未知状态”。

阶段 13 第三十三轮已补齐艺人编辑页审核与风险状态中文兜底：`/console/artists/[id]/edit` 对艺人审核状态和风险状态统一映射为中文，未知值分别显示“未知审核状态 / 未知风险状态”，不再把 `pending`、`normal` 或未来后端码值直接展示给后台用户。

阶段 13 第三十四轮已补齐客服账号未知角色中文兜底：`/console/support-accounts` 对未知 `supportRole` 显示“未知客服角色”，不再把后端新增或缺失角色误标为“普通客服”。

阶段 13 第三十五轮已补齐主办方入驻未知状态中文兜底：`/console/organizer-applications` 和 `/console/profile` 显式把 `status=2` 映射为“已驳回”，未知入驻状态显示“未知入驻状态”，不再默认误标为“已驳回”。

阶段 13 第三十六轮已补齐控制台个人中心账号状态中文兜底：`/console/profile` 按后端账号状态语义显示 `1=正常`、`0=已禁用`，其他值显示“未知账号状态”，不再把未知账号状态误标为“正常”。

阶段 13 第三十七轮已补齐客服账号启停状态中文兜底：`/console/support-accounts` 对客服账号 `status` 显式显示 `1=启用中`、`0=已停用`，其他值显示“未知账号状态”，启停按钮在未知状态下显示“状态待核对”并禁用，不再把未知账号状态误标为“已停用”或可直接“启用”。

阶段 13 第三十八轮已补齐 SeatCraft 座位属性状态中文兜底：座位图编辑控件的座位属性面板不再直接显示 `seat.status`，已知状态显示“可售 / 已锁定 / 已选中 / 已占用 / 已删除”，未知值显示“未知座位状态”，不改画布颜色、座位编辑限制或状态流转。

阶段 13 第三十九轮已补齐场次列表与导出状态中文兜底：`/console/sessions` 页面、CSV 与 Excel 导出统一复用 `formatConsoleSessionStatus()`，已知状态显示“启用 / 停用”，未知值显示“未知场次状态”，不再把未知场次状态误标为“停用”。

阶段 13 第四十轮已补齐主办方管理账号状态未知可见兜底：`/console/organizer-applications` 对未知或缺失的 `organizerStatus` 显示“未知主办方状态”，不再用 `return null` 和条件渲染把账号状态徽标静默隐藏。

阶段 13 第四十一轮已补齐客服审计与上下文未知码中文兜底：客服工作台和客服会话查询中的未知审计动作显示“未知操作”，未知上下文分区显示“未知上下文”，不再把 `FUTURE_ACTION` 或 `unknown` 这类后端码值原样展示。

阶段 13 第四十二轮已补齐客服标签未知码中文兜底：客服标签 formatter 对未知 `SupportTagCode` 显示“未知标签”，不再把后端新增标签码原样展示到客服工作台或会话查询。

阶段 13 第四十三轮已补齐活动艺人选择器缺名去编号化：`ActivityArtistSelector` 在已选艺人缺少名称时显示“艺人信息待同步”，不再用 `artistId` 拼“艺人 123”作为展示名称。

阶段 13 第四十四轮已补齐退款审核未知状态中文业务兜底：`/console/refunds` 页面、CSV 与 Excel 导出对未知退款状态统一显示“未知退款状态”，不再使用泛化“未知状态”，不改筛选、审核动作或接口字段。

阶段 13 第四十五轮已补齐平台主办方运营员账号状态中文兜底：`/console/organizer-admins` 对账号 `status` 显式显示 `1=启用中`、`0=已停用`，其他值显示“未知账号状态”，启停按钮在未知状态下显示“状态待核对”并禁用。

阶段 13 第四十六轮已补齐活动管理列表未知上下架状态保护：`/console/activities` 对未知活动销售状态显示“未知活动状态”，上下架操作标题显示“状态待核对”并禁用，不再把未知状态误标为“下架”或提供“上架”写动作。

阶段 13 第四十七轮已补齐评价审核未知状态业务兜底：`/console/activity-engagement` 的评价审核未知状态显示“未知评价状态”，与同页问答、举报未知状态口径一致，不再使用泛化“未知状态”。

阶段 13 第四十八轮已补齐异常任务未知状态业务兜底：异常任务状态 formatter 对未知状态显示“未知异常状态”，不再在异常任务队列里使用泛化“未知状态”。

阶段 13 第四十九轮已补齐对账未知状态业务兜底：对账批次、明细和差异状态 formatter 的未知兜底分别显示“未知对账批次状态 / 未知对账明细状态 / 未知对账差异状态”，页面和导出继续复用同一套中文映射。

阶段 11 第二十一轮已补齐候补未知状态业务兜底：`getWaitlistStatusLabel()` 对未知候补状态显示“候补状态同步中”，不再让 C 端候补列表展示泛化“未知状态”。

阶段 11 第二十二轮已补齐 C 端活动销售状态未知兜底：首页、搜索页和活动详情统一复用 `toActivitySaleStatus()`，已知 `0/1/2` 分别保持售罄/售票中/待开票语义，其他未知状态显示“状态同步中”，不再误标为“售罄”。

阶段 13 第五十轮已补齐私有文件上传类型缺省文案：`PrivateFileUpload` 在文件类型缺失时显示“文件类型待同步”，不再展示泛化“未知类型”。

阶段 13 第五十一轮已补齐场次未知状态待核对样式：`/console/sessions` 继续显示“未知场次状态”，并通过 `getConsoleSessionStatusClassName()` 使用独立待核对样式，不再和“停用”共用灰色徽标。

阶段 11 第二十三轮已补齐 C 端订单列表退款未知状态同步兜底：`/orders` 退款进度、人工客服介入文案和重复退款申请阻塞统一复用 `getRefundStatusMeta()` / `isRefundActionBlockingStatus()`，未知退款状态显示“退款状态同步中”并阻止继续发起新退款，不再直取 `REFUND_STATUS_MAP[latestRefund.status]` 导致页面崩溃或误导用户。

阶段 13 第五十二轮已补齐评价审核未知状态写动作保护：`/console/activity-engagement` 对未知评价状态继续显示“未知评价状态”，并将原先基于 `review.status !== 1/2` 的写动作改为已知状态 helper 判断；未知状态仅显示“状态待核对”，不再暴露“通过/隐藏”审核动作。

阶段 13 第五十三轮已补齐问答未知状态写动作保护：`/console/activity-engagement` 对未知问答状态继续显示“未知问答状态”，并将原先基于 `question.status !== 'HIDDEN'` / `=== 'HIDDEN'` 的写动作改为已知状态 helper 判断；未知状态仅显示“状态待核对”，不再暴露“保存回复/隐藏/恢复”问答写动作。

阶段 13 第五十四轮已补齐控制台订单未知状态待核对样式：`/console/orders` 继续通过 `formatConsoleOrderStatusLabel()` 显示“未知订单状态”，并通过 `getConsoleOrderStatusClassName()` 使用独立待核对样式，不再和“已取消/已退款”共用灰色徽标。

阶段 13 第五十五轮已补齐私有文件上传大小缺省文案：`PrivateFileUpload` 在文件大小缺失或无效时显示“文件大小待同步”，与已完成的“文件类型待同步”保持一致，不再展示泛化“未知大小”。

阶段 11 第二十四轮已补齐 C 端订单列表票档旧兜底：`/orders` 列表渲染不再保留“未知票档”，缺少票档名称时统一显示“票档信息待同步”，与订单详情、票夹和抢票上下文口径一致。

阶段 11 第二十五轮已补齐帮助中心转人工未知会话状态兜底：`/help` 转人工按钮改为复用 `formatSupportHandoffActionLabel()`，未知会话状态显示“状态待核对”，不再在按钮文案上落到“转人工客服”；转人工可点击条件仍由 `canRequestSupportHandoff()` 控制。

阶段 13 第五十六轮已补齐退款未知状态待核对样式：`/console/refunds` 继续通过 `formatConsoleRefundStatus()` 显示“未知退款状态”，并通过 `getConsoleRefundStatusClassName()` 使用独立待核对样式，不再和“已拒绝”共用灰色徽标。

阶段 13 第五十七轮已补齐入场核验未知结果待核对样式：`/console/check-in` 页面和 CSV/Excel 导出统一复用 `formatConsoleCheckInResult()`，页面通过 `getConsoleCheckInResultClassName()` 给未知核验结果使用独立待核对样式，不再在页面内联灰色兜底。

阶段 13 第五十八轮已补齐艺人列表未知审核/风险状态展示与写动作保护：`/console/artists` 统一复用 `console-artists.ts` 里的审核和风险状态 formatter，未知审核状态显示“未知审核状态”，未知风险状态显示“未知风险状态”，风险写动作显示“状态待核对”并禁用，不再把未知风险状态误标为“风险正常”或直接开放“列入风险”。

阶段 13 第五十九轮已补齐风险事件未知恢复状态写动作保护：`/console/risk-events` 在最新恢复申请状态未知时继续显示“未知审核状态”，并把“提交恢复申请”按钮降级为“状态待核对”且禁用；只有无最新申请或已知非审核中状态才允许提交恢复申请。

阶段 13 第六十轮已补齐风险案例未知恢复状态操作区兜底：`/console/risk-cases` 在最新恢复状态未知时继续显示“未知审核状态”，操作区显示“状态待核对”并使用待核对样式，不再把未知状态落到“等待主办方处理”。

阶段 13 第六十一轮已补齐客服会话未知状态业务兜底：`formatSupportConversationStatus()` 对未知客服会话状态显示“未知会话状态”，不再把后端新增会话状态误标为“处理中”；`/support` 和 `/console/support-conversations` 继续复用共享 formatter。

阶段 11 第二十六轮已补齐商户入驻页未知入驻状态兜底：`/merchant` 将 `status=2` 显式映射为“已驳回”，其他未映射入驻状态显示“未知入驻状态”，状态说明显示“入驻状态待核对，请稍后刷新或联系平台客服。”，不再把未知状态误标为“已驳回”或“资料正在审核中”。

阶段 11 第二十七轮已补齐活动详情问答未知状态回复兜底：`/activity/[id]` 问答列表在问题无回答且状态未知时显示“问答状态同步中”，不再把未知问答状态落到普通“暂无回复”；已知 `PENDING/ANSWERED/HIDDEN` 状态的展示语义保持不变。

阶段 11 第二十八轮已补齐 C 端订单活动/场馆缺名同步兜底：`/orders` 和 `/orders/[id]` 在活动名或场馆名缺失时显示“活动信息待同步 / 场馆信息待同步”，退款人工客服消息同样使用“活动信息待同步”，不再展示“未知活动 / 未知场馆”。

阶段 13 第六十二轮已补齐客服工作台未知会话状态写动作保护：`/support` 将接入、回复、标签、备注、转接、升级和申请结束等写动作统一切到客服会话状态 helper；未知会话状态仅显示待核对提示并禁用写动作，函数入口也会返回“会话状态待核对，请刷新后再操作”，不再只靠 `active.status === 'CLOSED'` 判断开放操作。

阶段 13 第六十三轮已补齐退款审核未知状态操作区兜底：`/console/refunds` 操作列改为复用退款状态 action helper，未知退款状态显示“状态待核对”，不再落到“无需操作”；已知待审核和处理中继续保留同意/拒绝/重试语义。

阶段 13 第六十四轮已补齐控制台订单活动缺名同步兜底：`/console/orders` 页面和订单 CSV 导出在活动名缺失时统一复用 `getConsoleOrderActivityLabel()` 显示“活动信息待同步”，不再展示“未知活动”；订单状态、票档兜底、筛选和导出字段保持不变。

阶段 13 第六十五轮已补齐退款审核活动缺名同步兜底：`/console/refunds` 页面、退款 CSV 和 Excel 导出在退款关联订单/活动名缺失时统一复用 `getConsoleRefundActivityLabel()` 显示“活动信息待同步”，不再展示“未知活动”；退款状态、审核动作、筛选和导出字段保持不变。

阶段 12 第八轮已补齐入场核验异常报表导出：`/console/check-in` 在既有全量核验记录 CSV/Excel 基础上新增异常 CSV/Excel 导出，筛出失败、重复和未知待核对结果，继续只导出请求号、票号、设备、渠道、结果、失败原因和核验时间，不导出 `ticketId`、`orderId`、`userId`、`sessionId`、`ticketTypeId`、`operatorUserId` 等内部关联编号。

阶段 12 第九轮已补齐活动列表批量下架并退款入口：`/console/activities` 当前页支持勾选可下架的普通活动/巡演并批量执行“下架并退款”，逐条复用既有 `deactivateActivity()` / `deactivateTour()` 链路，二次确认明确“同意退款”，结果汇总成功/失败数量和退款影响；草稿、风险停票、已下架巡演和未知销售状态不会进入批量写动作。批量操作审计暂未跨库硬写，后续如要完整审计应设计 `java-user` internal audit API。

阶段 12 第十轮已补齐批量下架操作审计 internal API：`java-user` 新增 `POST /api/user/internal/operation-audits`，使用 `X-Internal-Token` 写入全局 `operation_audit_log`；`java-ticket` 在普通活动和巡演“下架并退款”成功后通过 Feign 写入 `activity.deactivate.refund` / `tour.deactivate.refund` 审计，审计结果包含下架数量、已支付订单数、退款成功/失败/未知/需人工处理数量，审计失败只记录 warn，不影响下架退款主链路；前端操作审计列表已补齐对应中文展示。

阶段 12 第十一轮已补齐批量通知购票用户入口：`java-ticket` 新增 `POST /api/ticket/admin/activities/{id}/buyer-notifications`，只对单个普通活动拉取已支付订单并发送 `ACTIVITY_BUYER_NOTICE` 站内通知，`channels` 限定为 `IN_APP`，action 指向 `/orders/{orderId}`；同一活动同一订单的事件幂等键包含通知内容 hash，避免重复发送不同内容时被旧幂等键吞掉；成功后通过 `java-user` internal audit API 写入 `activity.buyers.notify` 审计，审计失败只 warn；`/console/activities` 支持勾选普通活动后批量填写通知内容并逐条调用接口，巡演不会误调单活动接口，通知中心已补“活动通知”中文类型。

阶段 12 第十二轮已补齐退款审核页批量处理入口：`/console/refunds` 支持勾选当前页可处理退款，批量同意/重试逐条复用 `approveRefund()`，批量拒绝仅允许待审核记录并逐条复用 `rejectRefund()`；处理中退款不会进入批量拒绝，未知退款状态不能被勾选；批量动作有二次确认、中文结果反馈和成功/失败计数，并继续沿用现有单条退款审核链路、通知和审计，不新增后端批量 API。

阶段 12 第十三轮已补齐场次页票档批量改价入口：`/console/sessions` 支持勾选当前页可改价票档，批量输入目标票价并逐条复用 `updateAdminTicketType(ticket.id, { price })`；仅 `status=0/1` 票档可进入批量改价，未知票档状态不会被勾选；批量动作有二次确认、目标票价校验、中文结果反馈和成功/失败计数，不新增后端批量 API。当前批量改价复用单条票档更新链路，后续如要求批量改价独立审计，应复用或扩展 `java-user` internal audit API。

阶段 12 第十四轮已补齐场次页票档批量启停入口：`/console/sessions` 在同一套当前页票档多选基础上新增“批量启用 / 批量停用”，逐条复用 `updateAdminTicketType(ticket.id, { status })`；仅已知 `status=0/1` 且目标状态不同的票档会进入批量状态更新，未知票档状态不会被勾选或写入；批量动作有二次确认、中文结果反馈和成功/失败计数，不新增后端批量 API。批量库存调整因现有 `totalStock` 单条链路缺少已售下限保护，已后置为独立切片。

阶段 12 第十五轮已补齐场次页票档批量库存调整入口：`java-ticket` 单条 `updateTicketType()` 对 `totalStock` 增加保护，新总库存不能小于 0，也不能小于已售数量，更新时按“已售 = 原总库存 - 原余票”重算余票；`/console/sessions` 在当前页票档多选基础上新增“批量库存”，逐条复用 `updateAdminTicketType(ticket.id, { totalStock })`，前端先校验非负整数并拦截目标总库存低于已售数量的票档；仅已知 `status=0/1` 票档进入批量库存目标，未知票档状态不会被勾选或写入；批量动作有二次确认、中文结果反馈和成功/失败计数，不新增后端批量 API。

阶段 12 第十六轮已补齐场次页票档批量导入入口：`/console/sessions` 新增“批量导入票档”，支持粘贴文本/CSV/Tab 数据，字段顺序为“场次编号,票档名称,票价,总库存”，允许第一行表头，前端统一校验场次编号、名称、票价和总库存；导入时逐条复用 `createAdminTicketType()` 既有单条创建链路，不新增后端批量 API，不接 SeatCraft 区域/座位绑定批量导入；批量动作有二次确认、中文结果反馈和成功/失败计数。

阶段 12 第十七轮已补齐票档创建/更新操作审计：`java-ticket` 在单条票档创建成功后写 `ticket_type.create` 审计，在单条票档更新成功后写 `ticket_type.update` 审计；批量导入、改价、启停和库存因复用单条创建/更新链路而自动进入审计；审计写入通过 `java-user` internal API 完成，失败只记录 warn，不阻断票档主链路；前端操作审计展示已补“创建票档 / 更新票档 / 票档”中文映射。

阶段 13 第六十六轮已补齐 RBAC 权限变更差异预览：`/console/roles` 在保存前展示“权限变更预览”，按新增/移除权限列出中文权限名和权限码；`rbac.manage` 与 `platform_super_admin` 角色变更被标记为“敏感权限变更”，保存弹窗统一为“确认更新角色权限”，取消弹窗不会触发权限写动作；差异计算已抽到 `frontend/src/lib/rbac-permission-diff.ts` 并有单元测试覆盖。本轮不改后端、不改数据库结构，沿用现有 `rbac.role_permission.update` 审计链路。

阶段 13 第六十七轮已补齐 RBAC 权限变更审计明细：`java-user` 的 `RbacAdminService.updateRolePermissions()` 返回真实更新前后权限差异，按新增/移除权限输出中文权限名和权限码；`InternalWorkbenchController` 写入 `rbac.role_permission.update` 审计时不再使用请求体权限数量，而是写入“新增权限 / 移除权限 / 更新后权限数”摘要。差异由后端基于数据库旧值和规范化后的新值计算，`platform_super_admin` 仍强制保存全部权限，最后一个 `rbac.manage` 角色保护保持不变。本轮不改数据库结构，不新增跨服务访问。

阶段 13 第六十八轮已补齐 RBAC 角色模板套用入口：`/console/roles` 在权限变更预览前展示“套用角色模板”，支持客服主管、普通客服、主办方和平台主办方运营员标准模板；模板只按当前权限表已存在的权限码更新页面勾选状态，不直接保存、不新增后端 API、不改数据库结构。`platform_super_admin` 不提供模板，避免和后端强制全权限语义冲突；最终保存仍走现有“权限变更预览 / 确认更新角色权限 / 后端真实差异审计”链路。

阶段 13 第六十九轮已补齐操作审计筛选中文化入口：`/console/audit-logs` 将动作和对象类型筛选从自由文本改为“全部操作类型 / 全部对象类型”中文下拉框；下拉选项由 `operation-display` 的动作/对象映射生成，显示中文业务标签，提交给 `listOperationAuditLogs()` 的 `action` / `targetType` 仍是后端原码值。本轮不新增后端 API、不改数据库结构、不触发写动作；日志列表列名同步调整为“操作类型 / 对象类型”，继续保留操作人编号、追踪编号等必要技术追踪语境。

阶段 13 第七十轮已补齐主办方运营审计风险等级引用中文化：`operation-display` 对 `organizer_ops_assignment` 的 `targetRef` 增加风险等级识别，历史或种子审计里的 `high` / `watch` 显示为“风险等级：高风险 / 风险等级：关注”，不再落到“跟进类型：high / watch”；负责人编号和跟进类型显示保持原语义。本轮只改前端展示 helper 和测试，不改后端审计写入、不改数据库结构。

阶段 13 第七十一轮已补齐艺人档案审计对象类型中文化：`operation-display` 将 `artist` 对象类型映射为“艺人档案”，`/console/audit-logs` 的对象类型筛选同步生成“艺人档案”选项，`ARTIST_REVIEW` 这类审计不再显示“未知对象”。本轮只改前端展示 helper 和测试，不改后端审计写入、不改数据库结构。

阶段 13 第七十二轮已补齐异常任务新建类型下拉中文化覆盖：`/console/exception-tasks` 的“新建异常任务”类型下拉改为复用 `operation-display` 的异常任务类型映射，覆盖本地已有 `PAYMENT_TIMEOUT`、`REFUND_UNKNOWN`、`TICKET_ISSUE`、`STOCK_SYNC`、`RISK_REVIEW`、`RECONCILE_DIFF` 等历史类型，显示中文标签但保留提交给后端的原码值。本轮只改前端展示 helper、页面入口和测试，不改后端接口、不改数据库结构。

阶段 13 第七十三轮已补齐对账历史码值中文化兼容：`operation-display` 的对账 formatter 兼容本地 seed 与历史批次里的 `ORDER`、`REFUND`、`different`、`REFUND_AMOUNT_MISMATCH` 等码值，页面和 CSV/Excel 导出显示“订单 / 退款 / 存在差异 / 退款金额不一致”，不再落到“未知业务类型 / 未知对账明细状态 / 未知差异类型”。本轮只改前端展示 helper 和测试，不改后端接口、不改数据库结构。

阶段 13 第七十四轮已补齐对账摘要中文字段展示：`operation-display` 的对账摘要 key 映射兼容新批次 summary JSON 中的 `业务日期`、`支付笔数`、`支付金额`、`退款笔数`、`退款金额`、`净额`、`差异数` 等中文 key，`/console/reconciliation` 批次列表不再把这些真实字段误显示为“其他指标”。本轮只改前端展示 helper 和测试，不改后端接口、不改数据库结构。

阶段 13 第七十五轮已补齐站点配置审核状态中文化和写动作保护：`operation-display` 新增站点配置状态 formatter 与可审核状态判断，`/console/station-config-reviews` 增加“状态”列，`submitted` 显示为“待审核”，非待审核或未来状态只显示“状态待核对”并阻断通过/驳回入口。本轮只改前端展示 helper、审核页和测试，不改后端接口、不改数据库结构。

阶段 13 第七十六轮已补齐异常任务队列状态操作区兜底：`operation-display` 新增异常任务可认领、可处理、可关闭和未结束状态 helper，`/console/exception-tasks` 的待处理/高优先级统计与操作列统一复用这些 helper；未知或未来状态显示“状态待核对”，事件入口阻断认领、处理和关闭写动作。本轮只改前端展示 helper、异常任务页和测试，不改后端接口、不改数据库结构。

阶段 13 第七十七轮已补齐评价举报未知状态操作保护：`/console/activity-engagement` 的评价举报 tab 新增举报状态 known/action helper，未知或未来举报状态显示“状态待核对”，处理/驳回事件入口返回“举报状态待核对，请刷新后再操作”，不再静默空操作区或误开放写动作。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第七十八轮已补齐场馆资料审核未知状态操作保护：`/console/venue/applications` 新增场馆审核状态 known/reviewable helper，未知或未来状态显示“状态待核对”，审核入口和通过/驳回提交前二次检查返回“场馆审核状态待核对，请刷新后再操作”，不再只靠 `item.status === 0` 暴露写动作。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第七十九轮已补齐主办方入驻审核未知状态操作保护：`/console/organizer-applications` 新增入驻申请状态 known/reviewable helper，未知或未来入驻状态显示“状态待核对”，通过/驳回事件入口返回“入驻审核状态待核对，请刷新后再操作”，不再只靠 `item.status !== 0` 禁用按钮。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十轮已补齐恢复售票审核未知状态操作保护：`/console/risk-resolutions` 新增恢复售票审核状态 known/reviewable helper，未知或未来状态显示“状态待核对”，通过/拒绝事件入口返回“恢复售票审核状态待核对，请刷新后再操作”，不再只靠 `item.status === 'pending'` 暴露审核写动作。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十一轮已补齐取消主办方未知主办方状态操作保护：`/console/organizer-applications` 新增主办方账号状态 known/cancel helper，只有入驻已通过且 `organizerStatus=1` 的有效主办方可执行取消；未知或未来主办方状态显示“状态待核对”，取消入口返回“主办方状态待核对，请刷新后再操作”，不再只靠入驻申请 `status=1` 和 `role=user` 判断取消写动作。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十二轮已补齐对账差异未知状态操作保护：`operation-display` 新增对账差异状态 known/open helper，`/console/reconciliation` 的差异记录未知或未来状态显示“状态待核对”，处理/忽略事件入口返回“对账差异状态待核对，请刷新后再操作”，不再把未知差异状态落到“已结束”或只靠 `diff.status === 'open'` 分支判断。本轮只改前端展示 helper、页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十三轮已补齐艺人档案审核未知状态操作保护：`console-artists` 新增艺人审核状态 known/reviewable helper，`/console/artists/pending` 显示“审核状态：待审核”，未知或未来审核状态显示“状态待核对”，通过/拒绝/标记风险事件入口返回“艺人审核状态待核对，请刷新后再操作”，不再只靠待审核接口隐含状态或只传 `artistId` 执行写动作。本轮只改前端展示 helper、页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十四轮已补齐评价问答审核事件入口状态保护：`/console/activity-engagement` 的评价审核和购前问答写动作入口改为接收完整记录并复核状态，未知或未来评价状态返回“评价状态待核对，请刷新后再操作”，未知或未来问答状态返回“问答状态待核对，请刷新后再操作”，不再只靠按钮显隐或只传 `review.id` / `question.id` 执行写动作。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十五轮已补齐平台主办方运营员账号未知状态编辑/启停保护：`/console/organizer-admins` 新增账号状态 known/enabled helper，未知或未来账号状态显示“状态待核对”，编辑入口和启停入口返回“账号状态待核对，请刷新后再操作”，未知状态图标不再误落到“启用”勾选图标。本轮只改前端页面和测试，不改后端接口、不改数据库结构。

阶段 13 第八十六轮已补齐客服账号未知状态编辑/启停保护：`/console/support-accounts` 新增客服账号状态 known/enabled helper，未知或未来账号状态显示“状态待核对”，编辑入口和启停入口返回“账号状态待核对，请刷新后再操作”，未知状态图标不再误落到“启用”勾选图标；同时修复本地 `127.0.0.1:3000` 验收时 Next dev HMR 被跨源拦截的问题，`next.config.ts` 显式允许 `127.0.0.1` 作为开发来源。本轮不改后端接口、不改数据库结构。

阶段 13 第八十七轮已补齐退款审核单条审核状态二次保护：`/console/refunds` 单条同意/重试和拒绝入口改为基于完整退款记录复核状态，提交备注前再按当前列表记录和动作类型二次校验；待审核 `status=0` 可同意或拒绝，处理中 `status=4` 只可同意/重试，未知、已退款、已拒绝或已结束状态统一返回“退款状态待核对，请刷新后再操作”，不再只靠打开弹窗时的按钮可见性保护退款写动作。本轮只改前端页面、退款 helper 和测试，不改后端接口、不改数据库结构。

阶段 13 第八十八轮已补齐帮助中心未知会话状态发送保护：`/help` 在线客服输入框和发送入口改为复用客服会话状态 helper；没有会话时仍可创建新会话，已有会话只有已知且未结束状态可继续发送，未知或未来状态显示“会话状态待核对，请刷新后再操作”并阻断发送写接口，不再只靠 `conversation?.status === 'CLOSED'` 判断。本轮只改前端帮助页和测试，不改后端接口、不改数据库结构。

阶段 13 第八十九轮已补齐帮助中心确认结束会话状态二次保护：`/help` 的“确认结束会话”入口改为复用 `canConfirmSupportConversationClose()`；只有 `CLOSE_REQUESTED` 可确认结束，未知或未来状态返回“会话状态待核对，请刷新后再操作”，已知但不可确认状态返回“当前会话暂不能结束，请刷新后再操作”，不再只靠按钮渲染条件保护确认结束写动作。本轮只改前端帮助页、客服状态 helper 和测试，不改后端接口、不改数据库结构。

阶段 13 第九十轮已补齐帮助中心转人工会话状态二次保护：`/help` 的“转人工客服”事件入口改为在调用 `handoffSupportConversation()` 前复核 `canRequestSupportHandoff()`；已有会话但当前状态不可转人工时返回“会话状态待核对，请刷新后再操作”或“当前会话暂不能转人工，请刷新后再操作”，不再静默 return。本轮只改前端帮助页和测试，不改后端接口、不改数据库结构。

阶段 13 第九十一轮已补齐客服工作台已知但不可执行状态的写动作反馈：`/support` 的统一写动作守卫 `canProceedWithActiveWrite()` 在状态不允许当前动作时，未知状态继续返回“会话状态待核对，请刷新后再操作”，已知但不可执行状态返回“当前会话暂不能执行该操作，请刷新后再操作”，不再只对未知状态提示、对已知不可执行动作静默 return。本轮只改前端客服工作台和测试，不改后端接口、不改数据库结构。

阶段 13 第九十二轮已补齐风险事件恢复申请状态阻断反馈与主办方入口守卫修正：`/console/risk-events` 对最新恢复申请未知状态和审核中状态分别返回中文阻断提示，并在提交前按当前列表状态二次复核；`ConsoleLayout` 对 `role=organizer` 的业务路径优先走主办方路径守卫，避免带 `permissionCodes` 的主办方被平台权限码过滤误重定向。本轮只改前端风险事件页、控制台 layout 和测试，不改后端接口、不改数据库结构。

阶段 13 第九十三轮已补齐平台运营摘要链路健康卡片：`/console` 运营驾驶舱新增“摘要链路健康”，按 ticket/payment/grab/workbench 聚合错误映射为“票务摘要链路 / 退款摘要链路 / 抢票摘要链路 / 工作台摘要链路”，正常显示“摘要链路正常”，异常显示“状态待核对”和聚合错误中文提示，未知 source 归入“其他摘要链路”且不暴露原始来源码。本轮只改前端控制台首页、摘要链路 helper 和测试，不改后端接口、不改数据库结构；该卡片只代表 `PlatformOpsSummaryVO.errors` 摘要聚合链路，不代表 Nacos/Seata/Redis/RabbitMQ 等真实基础设施探针。

阶段 13 第九十四轮已补齐平台基础设施健康探针：`java-user` 的平台运营摘要新增 `infrastructureHealth`，独立探测 Nacos HTTP、Redis TCP、RabbitMQ TCP 和 Seata TCP，可返回“正常 / 状态待核对 / 未配置”；`/console` 新增“基础设施健康”卡片，缺少探针字段时显示“未配置 / 基础设施探针未配置”，不把摘要链路健康等同于基础设施健康。已修复 IDEA 启动 `java-user` 时 `PlatformInfrastructureHealthProbe.<init>()` 无参构造缺失问题，生产构造函数显式 `@Autowired`，并补 Spring 容器创建测试；用户在 IDEA 重启 `java-user` 后，网关接口和浏览器页面均已验证四项基础设施健康正常。本轮不改数据库结构、不新增依赖。

## 生产运维后置项

这些事项依赖真实外部项目、真实凭据或外部沙盒稳定性，不再阻塞当前本地总路线：

- Sentry：生产运维上线前注入真实 DSN，触发少量试点异常，确认项目侧事件可见且敏感字段已脱敏。
- PostHog：生产运维上线前配置真实 token/host，确认 enabled 态浏览器 Network 和项目侧 allowlist 事件可见，继续关闭 autocapture 和 Session Replay。
- Alipay sandbox：外部沙盒稳定后复测同意退款链路，每次实测前恢复固定 seed 基线。

## 二期增强候选

- 客服主管：SLA、满意度、质检、排班和接待容量。
- 平台主办方运营：高风险双人复核、按负载自动分配、跨 ticket/order 的主办方运营摘要。
- 入场核验：异常补录、备用扫码页、设备/工作人员 Gateway 鉴权。
- 后台中文化：站点变更审核、日志列表、异常任务队列、对账批次列表不直接展示英文枚举或后端字段名，统一映射为中文业务标签。
- 链路耗时治理：AI 客服首字响应、模型回落原因、抢票排队、锁票、建单、支付同步和出票各阶段分别记录耗时，不把所有慢响应都归因到网关。
- 通知体系：真实 SMS/Email 渠道适配器、投递记录、重试和失败告警。
- 搜索与推荐：ES 活动检索、后端推荐 API、PostHog 稳定后的实时反馈闭环。

## 后续执行规则

- 不在仓库写入真实 DSN、token、host、密钥或外部账号凭据。
- 不使用 mock DSN、占位 token、假 transport 或本地日志替代外部 SaaS 项目侧验收。
- 不恢复动态系统、`MomentSection`、`SocialController`、moment API 或旧 social/moment 持久化边界。
- 不恢复生产入口的 mock/offline 降级。
- 涉及数据库结构变更时必须同步 SQL 迁移、本地库执行和验证命令。
- 后端能力对用户可见时必须同步前端入口、状态、权限和浏览器验证。
